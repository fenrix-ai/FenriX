# W05 completion — court lineup interaction and accessible placement

- Branch: `codex/ss-ui-w05`
- Frozen base: `b48cdf620c594aba0386fa6775af86aea40da401`
- Implementation commit: `d5bf96c` (`feat(salary-showdown): build accessible lineup court`)
- I01 correction starting HEAD: `21c8bca2fb67f44268fe06579f76106301d6398f`
- Correction implementation commit: `71521d3f1ae309449466981fd0cc46dd8a1d54d5` (`fix(salary-showdown): scope lineup save state`)
- Final note-inclusive HEAD: the commit containing this file; the worker report supplies the resolved SHA because a commit cannot contain its own hash.
- Worktree: `/private/tmp/salary-showdown-ui-worktrees/W05`
- Emulator project: `salary-showdown-dev` only; Auth `9199`, Firestore `8180`, Functions `5101`

## Implementation

- Rebuilt Lineup as a laptop workspace with a responsive half court, adjacent roster and playstyle rail, and distinct sixth-player, two-player active bench, and inactive-depth zones.
- Added click and keyboard selection, Escape cancellation, separate accessible Select and Drag controls, legal-target highlighting, explicit rejection announcements, target focus after placement, and reduced-motion-safe accepted-placement emphasis.
- Kept `place` as the sole legality check for click and drag. Occupied swaps validate the displaced player's return slot and leave state unchanged on rejection.
- Preserved locked lineups verbatim on remount, all lineup pids, active bench ordering, and later depth ordering in `toLineup`; only `bench[0]` and `bench[1]` are labelled active and later entries are labelled zero minutes.
- Preserved role fallback through `actsAs('Coach')`, server-authoritative `submitLineup`, revisable locked state, exact playstyle names/blurbs, and team-visible state only. Successful saves use the frozen scoped receipt hook and do not auto-submit local swaps.
- Keyed the complete local lineup workspace to game, round, phase, team, and the actual authenticated uid. A scope change now clears drafts, pending state, errors, placement announcements, and receipts together, so late success or rejection from an old game/user cannot alter the new workspace. Same-scope edits and locked-lineup remount behavior remain unchanged.

## Changed files

- `games/salary-showdown/app/src/pages/LineupPage.tsx`
- `games/salary-showdown/app/src/pages/LineupPage.module.css`
- `games/salary-showdown/app/src/components/lineup/CourtSlot.tsx`
- `games/salary-showdown/app/src/components/lineup/LineupRoster.tsx`
- `games/salary-showdown/app/src/components/lineup/PlacementStatus.tsx`
- `games/salary-showdown/app/src/components/lineup/LineupControls.module.css` (task-local addition)
- `games/salary-showdown/app/src/components/lineup/CourtSlot.test.tsx`
- `games/salary-showdown/app/src/itest/lineup.itest.tsx`
- `docs/superpowers/plans/salary-showdown-ui-tasks/completions/W05.md`

No manifests, lockfiles, shared foundation files, backend files, global CSS, shared types, contexts, routing, or other task files changed.

## Test-first evidence

- Baseline before edits: `npm test` — PASS, 20 files / 110 tests.
- The packet's initial `CourtSlot` regression first failed because `CourtSlot` did not exist, then failed behaviorally because Enter did not call `onPlace`.
- The page regressions then failed for the intended missing behavior: there were no selectable roster controls, accessible court targets, conflict announcement, Escape cancellation, or scoped saved-state announcement.
- The emulator regressions failed before implementation because the accessible active-bench/depth controls did not exist. Fresh DEV games were used and mutations ran sequentially.
- Browser verification exposed a keyboard conflict where dnd-kit captured Enter on the combined select/drag control. A regression requiring distinct Select and Drag buttons failed, then passed after the controls were separated.
- One post-implementation integration rerun failed 1/4 because its pid suffix query matched both the new Select and Drag buttons. The selector was narrowed to the Select control; no product behavior or assertion was weakened. The following final run passed.
- I01's external review fixture first reproduced the correction defects at 6 passed / 2 failed: a pending save acknowledged a different uid with unchanged membership labels, and a pending save published success after the game changed.
- Three owned deferred-call regressions first ran at 6 passed / 3 failed, covering uid-change success, game-change success, and game-change rejection plus pending cleanup. They pass after the scoped-workspace correction.

## Final verification

- `npm test -- src/components/lineup/CourtSlot.test.tsx src/lib/slots.test.ts` — PASS, 2 files / 11 tests.
- Correction targeted rerun of the same command — PASS, 2 files / 14 tests.
- `npx vitest run --config /private/tmp/salary-showdown-ui-worktrees/evidence/i01-w05-review/vitest.review.config.mjs` — PASS, 1 file / 8 tests, including both I01 review regressions.
- `npx vitest run --config vitest.integration.config.ts src/itest/lineup.itest.tsx` — PASS, 1 file / 4 tests, 9.02s. Non-fatal existing Firebase Admin `MetadataLookupWarning` only.
  - Fresh final-pass game IDs: `fKTR2uzXyk7Tr0P5DDnJ`, `GanO87RghgsL46qqaLzG`, `I6Fr0Hy9R2J05gRllkLD`, `uluPYTK34GHeRHsPWOqB`.
- Correction integration rerun of the same command — PASS, 1 file / 4 tests, 8.48s. The same non-fatal Firebase Admin `MetadataLookupWarning` appeared.
  - Fresh correction game IDs: `sa04Jsxs7NTVGeJxa5LN`, `QKVqPUXQsnTAXJQJzwbw`, `K504utD1c0kS7HrqV6bA`, `I6yxkoKbdg0gBWzcZF1X`.
- `npm test` — PASS, 21 files / 116 tests.
- Correction full `npm test` — PASS, 21 files / 119 tests, 3.94s.
- `npx tsc -b --pretty false` — PASS, exit 0.
- `npm run audit:ui` — PASS, 86 files scanned.
- `git diff --check` — PASS.

## Browser and layout evidence

- Started exactly with `npx vite --host 127.0.0.1 --port 5187 --strictPort`.
- Fresh DEV browser game: `qzkRqUXd3xx3dUW91OEo` / `QZKRQU`.
- Browser keyboard path selected an active-bench player, placed it in a legal starter slot, retained focus at that slot, saved, reloaded, and reproduced the exact same slot pid sequence.
- Browser pointer drag moved a legal active-bench player to a starter slot, focused the target, announced the swap, and saved the revision.
- Measured 1024×768, 1280×720, 1366×768, and 1440×900: no horizontal document overflow. The court scales from 450px to 620px tall alongside the roster/playstyle rail; reserve slots had non-overlapping bounds. Browser console errors/warnings: none.
- Correction browser rerun started with the required `npx vite --host 127.0.0.1 --port 5187 --strictPort`. It joined the open Scout seat in fresh correction game `I6yxkoKbdg0gBWzcZF1X` / `I6YXKO`, rendered all ten active pids with two active bench entries and two ordered inactive-depth entries, and retained the locked/revisable state. Rechecked 1024×768, 1280×720, 1366×768, and 1440×900 with no horizontal overflow or reserve overlap; browser console errors/warnings: none.
- Deep-roster coverage is in the passing emulator test: ten roster pids survive submission, `bench` has four entries, and the two later entries remain ordered after the active pair.
- Screenshot paths: None. No screenshot file was saved, per the deferred final visual-review direction.

## Pending final I01 acceptance checks

- Saved before/after screenshots.
- Actual 200% browser zoom inspection.
- Actual operating-system/browser reduced-motion preference toggle.
- Physical trackpad drag and full integrated visual/classroom review across all screens.
- Dedicated long-name visual inspection in the integrated candidate.

These checks are pending, not passed. Automated reduced-motion CSS/hooks and static reflow checks are supporting evidence only.

## Interface deviations

None. `CourtSlot` keeps the frozen signature `CourtSlot({ label, pid, eligible, onPlace, children? })`. No shared interface or contract exception was needed.

## Emulator slot

W05 used the exclusive mutable slot only after checking the LIVE I01 ledger and confirming hub4400/Auth9199/Firestore8180 readiness with HTTP 200 and Functions5101 readiness with the expected root HTTP 404 through the approved localhost path. The correction used only `salary-showdown-dev`, fresh game IDs, and sequential mutable checks. It did not restart or reconfigure emulators, clear shared data, change the browser-SDK alias, or use production. W05 explicitly releases the mutable emulator slot with this revised completion.
