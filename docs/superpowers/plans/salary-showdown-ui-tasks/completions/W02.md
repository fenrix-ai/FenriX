# W02 completion — Market workspace, comparison and signing

Status: implemented; local behavior and emulator checks pass. Deferred integrated visual acceptance remains pending I01. 2026-09-17.

## Provenance and scope

- Prepared worktree: `/private/tmp/salary-showdown-ui-worktrees/W02`.
- Branch: `codex/ss-ui-w02`.
- Verified clean starting HEAD: `b48cdf620c594aba0386fa6775af86aea40da401`, exactly released by the LIVE I01 ledger.
- Implementation commit: `ef8f6b146fb461e9bd22a0b3eb6fce5e8eae5e2c`. The next documentation-only commit includes this note; final note-inclusive HEAD is reported in the handoff message.
- Inline implementation, no subagents, new sessions, worktrees, merge, deployment or backend changes. Manifests, lockfiles, shared foundations and browser-SDK alias unchanged.
- Interface deviations: **None**. CompareTray preserves the exact published props. Additional tests remain tightly local inside the owned market directory, as permitted by the packet.

## Changed files

Under `games/salary-showdown/app/src/`:

- `pages/FreeAgencyPage.tsx`
- `pages/FreeAgencyPage.module.css`
- `components/market/CompareTray.tsx`
- `components/market/CompareTray.test.tsx`
- `components/market/ContractComposer.tsx`
- `components/market/ContractComposer.test.tsx` (local addition)
- `components/market/MarketWorkspace.test.tsx` (local addition)
- `components/market/RosterRail.tsx`
- `itest/market.itest.tsx`

Plus this completion note only.

## Implemented behavior

Persistent catalog/detail workspace; keyboard buttons for selection and sorting; sticky table headings/player names; search, position, price and tonight/all filters preserved. Compare controls sit beside names. Up to three pids survive sorting/filtering; a fourth produces an inline limit message without replacing selections. Removal focuses a surviving control or the comparison heading. Comparison presents raw catalog fields, including all three previous-season fields, and star/half-star hype only.

Contract duration uses existing ask/rate/round helpers. Guaranteed total, per-round charge and five-round shared PayrollTimeline accompany signing, including future cap warnings. Saved owned contracts are not double-counted as preview candidates. Draft duration is retained per pid. Shared PlayerCard, PayrollTimeline, useActionReceipt and PhaseHeader APIs are unchanged.

Signing uses the existing callable and role fallback. In-flight guard prevents duplicate calls; pending state is attached to the action. Pending uses guarded aria-disabled so native browser focus remains on the signing button. No optimistic roster/payroll change. Scoped success receipt preserves the required ordinary-FA note; rejection retains inputs and existing error copy. Brief accepted-card/roster emphasis comes only from an accepted action, with reduced-motion CSS. Selecting another detail clears old emphasis. Done remains server-derived and revisable.

Roster facts include min/max and position counts. At 1280/1366 the labelled disclosure is above the workspace; 1440 adds a 240px roster rail while retaining an 814px inner catalog. At 1024 and below the detail follows the catalog in document flow. No shared listeners or private opponent reads added.

## Verification and evidence

All app commands ran from the W02 app directory. Evidence directory: `/private/tmp/salary-showdown-ui-worktrees/evidence/w02/`.

| Check | Actual outcome |
| --- | --- |
| Baseline `npm test` | PASS, 20 files / 110 tests |
| New preimplementation integration regressions | Expected FAIL: missing compare controls and duration aria-pressed; original Done test passed |
| Initial component runs | Failed imports before new components existed; not counted as assertion-level regression proof |
| Focus regression before fix | Expected FAIL: pending button lacked aria-disabled; native browser had independently reproduced focus loss |
| `npm test -- src/components/market/CompareTray.test.tsx` (included in component/full runs) | PASS, 2 tests |
| Final `npm test` | PASS, 23 files / 117 tests (`w02-unit-verified.log`) |
| Final `npx vitest run --config vitest.integration.config.ts src/itest/market.itest.tsx` | PASS, 1 file / 4 tests, 9.43s (`w02-integration-verified.log`) |
| Final `npx tsc -b --pretty false` | PASS, exit 0 (`w02-types-verified.log`) |
| Final `npm run audit:ui` | PASS, 85 files (`w02-audit-verified.log`) |
| `npm run build` | PASS; existing >500kB bundle warning (`w02-build.log`) |
| `git diff --check` | PASS |

The first sandboxed emulator attempt failed at Auth network access before assertions. Re-running with authorized local-network access succeeded in reaching the expected new regressions. A later typecheck caught unsupported Testing Library `exact` options in added tests; removed those options (string role names already match exactly), then build and standalone typecheck passed. These intermediate failures remain in logs. Final integration emits the existing non-fatal Firebase Admin MetadataLookupWarning; no assertion retries or config changes were introduced.

Unit cases cover raw/missing/zero comparison data, removal pid, three-column cap, future cap arithmetic, final-round duration, role/availability/pending gates, deferred-call duplicate prevention, no optimistic updates, exact signing note, saved-payroll preview, rejected-input retention, user-scope late receipt suppression, synthetic exclusion and unsold-star asks.

Integration cases preserve 112 tonight / 150 catalog, synthetic exclusion, live roster, exact nonexclusive note semantics, ALREADY_SIGNED, persisted Done; add another team's ordinary signing, three/four compare behavior across sort/search, removal focus, fallback signing after Done, actual GM seat arrival revoking fallback, and a constructed later-round dead-money fixture blocking only the longer offer.

Final integration fresh game IDs:
- `kHFt78NC1FvR8bunWlgA` — signing/nonexclusive/rejection.
- `rbADN7aptrYsimdTrEc6` — Done remount.
- `yZou7ikMOL9DJ4PeX4OR` — comparison/role fallback.
- `6BmIWhX3zJtRxqUEcqZN` — future cap.

Browser checks: `/private/tmp/salary-showdown-ui-worktrees/evidence/w02/browser-checks.md`. Fresh fixtures `BnwhCwFsrerL0u54fv0X` (round 1) and `jTzjAXsFZw3BLtxJ6lX8` (round 5). Tested compare three/four, sorting with detail open, removal focus, keyboard selection/signing, duration, Done then signing, live roster, duplicate rejection, narrow roster disclosure and one-round-only late offer. Successful post-fix keyboard signing retained Confirm signing focus. Captured console error lists were empty. Temporary tabs closed and viewport reset.

Read-only rendered DOM measurements showed document scrollWidth equal to viewport at 1280×720, 1366×768, 1440×900, 1024×720 and 640×360. At 640, market clientWidth 618 / scrollWidth 795, so overflow belonged to the table region. Exact measurements are in browser-checks.md.

## Pending final acceptance

- Full integrated visual/classroom review, saved screenshot artifacts, actual 200% browser zoom and actual reduced-motion preference toggles remain **pending**, per the user's deferral. Screenshot paths: **None**. An inline image was inspected but was clipped by the host preview panel; it is not full screenshot evidence. 640×360 is reflow evidence, not actual zoom.
- Full all-screen emulator/season/classroom suites belong to I01 and were not run by W02. Existing shared-service timeout history and Node runtime parity remain coordinator limitations.
- No contract exceptions or source blockers identified. This branch is ready for coordinator review, not a claim of final integrated acceptance.

## Emulator slot release

**W02 releases its exclusive mutable emulator slot on completion of this report.** All suites and browser mutations finished sequentially against `salary-showdown-dev` (Auth9199 / Firestore8180 / Functions5101). Service listeners were checked before use. No emulator restarted, reconfigured or cleared; no HTTP callable/backend change needed. I01 may grant the next worker's slot. W02 will perform no further emulator mutation without a new grant.
