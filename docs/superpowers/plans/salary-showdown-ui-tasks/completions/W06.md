# W06 completion report — simulation broadcast and completed matchup inspection

Status: implementation and emulator integration complete; deferred integrated visual acceptance pending.

## Commits and scope

- Branch: `codex/ss-ui-w06`
- Frozen base: `b48cdf620c594aba0386fa6775af86aea40da401`
- Implementation commit: `d2f03cc52dde5221f06f06e4fa6b65453fe3a3f4`
- Final note-inclusive HEAD: resolve with `git rev-parse HEAD` after this note commit; the direct I01 handoff supplies the immutable value because a commit cannot contain its own SHA

Owned implementation and test files:

- `games/salary-showdown/app/src/pages/SimulatePage.tsx`
- `games/salary-showdown/app/src/pages/SimulatePage.module.css`
- `games/salary-showdown/app/src/components/simulation/MatchReveal.tsx`
- `games/salary-showdown/app/src/components/simulation/CompletedGames.tsx`
- `games/salary-showdown/app/src/components/simulation/MatchBoxscore.tsx`
- `games/salary-showdown/app/src/components/simulation/SimulatePage.test.tsx`
- `games/salary-showdown/app/src/lib/matchRows.ts`
- `games/salary-showdown/app/src/lib/matchRows.test.ts`
- `games/salary-showdown/app/src/itest/simulate.itest.tsx`
- `docs/superpowers/plans/salary-showdown-ui-tasks/completions/W06.md`

## Implemented behavior

- Replaced the page-local simulation timer with the frozen `useRoundPresentation` prefix, so the score, own-team record, completed-game strip, and terminal state advance from one shared source.
- Added a preparing-results state that keeps the shared franchise header and current round visible while the round document is absent.
- Added a next-matchup/final-score broadcast panel, a completed-only horizontal matchup strip, and explicit Reveal all control wired only to `presentation.revealAll`.
- Added completed-match expansion with all 23 actual server boxscore columns. `matchRows` filters by `game_id`, so duplicate franchise names cannot mix separate games.
- Team identity is resolved from `GameResult.home` and `GameResult.away`. Matching display names show a neutral ambiguity notice and the full matchup instead of claiming name-based ownership.
- Added responsive reflow and reduced-motion CSS. Only the score arrival receives decorative motion; no second progression clock or fictional event feed exists.

## Test evidence

TDD RED evidence before implementation:

- `npm test -- src/lib/matchRows.test.ts src/components/simulation/SimulatePage.test.tsx`
- Expected failure: 2 files failed / 4 tests failed. `matchRows` returned both game IDs; the legacy page lacked the preparing status, prefix-driven record, completed-game expansion, and duplicate-name notice.

Local GREEN evidence from `games/salary-showdown/app`:

- `npm test -- src/lib/matchRows.test.ts src/components/simulation/SimulatePage.test.tsx` — PASS, 2 files / 4 tests.
- `npm test -- src/lib/matchRows.test.ts src/lib/liveStandings.test.ts src/components/simulation/SimulatePage.test.tsx` — PASS, 3 files / 9 tests.
- `npm test -- --no-file-parallelism` — PASS, 22 files / 114 tests.
- `npx tsc -b --pretty false` — PASS, exit 0.
- `npm run audit:ui` — PASS, 86 files scanned.
- `npm run build` — PASS; final run transformed 120 modules and emitted the existing chunk-size warning for the 939.38 kB main bundle.
- `git diff --check b48cdf620c594aba0386fa6775af86aea40da401..d2f03cc52dde5221f06f06e4fa6b65453fe3a3f4` — PASS.
- First restricted-sandbox integration attempt: FAIL, 1 file / 2 tests, both at anonymous-auth setup with `auth/network-request-failed`; no test reached a mutation. This matched the ledger's documented localhost sandbox restriction and is retained as a failed attempt, not a product failure or pass.
- Authorized localhost rerun with unchanged source/config: `npx vitest run --config vitest.integration.config.ts src/itest/simulate.itest.tsx` — PASS, 1 file / 2 tests in 5.48s. The known non-fatal Firebase Admin `MetadataLookupWarning: fetch failed` appeared.

## Pending checks and evidence boundaries

- Emulator integration used two fresh `salary-showdown-dev` games: `rYNXTt147dmocq9QQfsq` (join code `RYNXTT`) and `K34oSOKgk1x1BcqqPPKk` (join code `K34OSO`). IDs were read back from the local emulator after the harness completed because the suite itself does not print them.
- Full integrated visual review, saved screenshots, actual 200% browser zoom, real reduced-motion preference toggles, and final two-team/21-team classroom inspection are deferred to I01 by user direction and remain pending, not passed.
- Screenshot paths: None. No browser mutation was authorized or performed.
- Interface deviations: None.
- Shared API changes: None.
- Emulator slot status: released in the direct I01 completion handoff after the final note commit. W06 did not restart, reconfigure, or clear the shared services.
