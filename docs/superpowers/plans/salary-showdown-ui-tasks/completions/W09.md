# W09 completion — professor control desk

Status: implementation and reserved-emulator verification complete. Full integrated visual/classroom acceptance remains deferred to I01 as directed by the live ledger.

## Provenance

- Branch: `codex/ss-ui-w09`
- Exact frozen base: `b48cdf620c594aba0386fa6775af86aea40da401`
- Implementation commit: `741a1f3bb3cd3162fda1f0465d40d72ae1eaf1d7`
- Final note-inclusive HEAD: the commit containing this note; the worker response reports its resolved immutable SHA.
- Work stayed inline in the coordinator-prepared W09 worktree. No reset, rebase, merge, deployment, new worktree, or subagent/session was used.

## Changed files

- `games/salary-showdown/app/src/pages/professor/ProfessorPage.tsx`
- `games/salary-showdown/app/src/pages/professor/ProfessorPage.module.css`
- `games/salary-showdown/app/src/components/professor/AdvanceControl.tsx`
- `games/salary-showdown/app/src/components/professor/SessionSetup.tsx`
- `games/salary-showdown/app/src/components/professor/RevealStepper.tsx`
- `games/salary-showdown/app/src/components/professor/SubmissionGrid.tsx`
- `games/salary-showdown/app/src/components/professor/TimerStrip.tsx`
- `games/salary-showdown/app/src/components/professor/RoundContext.tsx`
- `games/salary-showdown/app/src/components/professor/SeatPanel.tsx`
- `games/salary-showdown/app/src/components/professor/ProfessorDesk.module.css`
- `games/salary-showdown/app/src/itest/professor.itest.tsx`
- `games/salary-showdown/app/src/itest/professor-seats.itest.tsx`
- `games/salary-showdown/app/src/itest/professor-fixes.itest.tsx`
- This completion note.

No manifest, lockfile, shared foundation/context, backend source, routing, global stylesheet, emulator configuration, or another task's file changed.

## Implementation

- Rebuilt `/professor` as a wide navy/gold control desk with the join code, transition-gated phase context, read-only cap/round settings, a 21-franchise readiness board, and an adjacent sticky control rail.
- Every franchise card shows the public `FranchiseMark`, numeric-aware team order, all three role seats, and the current Done/submission signal. The copy states that revisions remain open until phase advance; no private bid contents are rendered.
- Seat administration is now a secondary dialog opened globally or from a franchise card. It exposes one team at a time, keeps the existing two-step release, focuses the close control on open, supports Escape, and restores focus to the exact trigger on close.
- Added a synchronous single-flight guard to advance and stuck-recovery calls so two clicks in one render frame invoke the callable once. Normal advance still sends `expectedPhase` and `expectedRound` from the transition-gated game; only the existing recovery path uses raw state.
- Preserved advisory timer behavior, `STEP_TITLES`, season CSV bytes, create/start flow, phase confirmations, stuck recovery, and the existing professor context subscriptions/callables.
- Added responsive two-column, single-column, and 1024px fallbacks. The rail stays sticky while the long readiness board scrolls and becomes static when the layout collapses.

## Verification evidence

Commands ran from `games/salary-showdown/app` unless stated otherwise.

- TDD red run of the packet's three professor integration files: expected FAIL, 3 failures / 10 passes. It reproduced two same-frame advance calls, the missing 21-team readiness board, and the missing seat-tools trigger/focus flow. The new transition-marker heading regression already passed and preserved the frozen gate.
- `npm test -- --no-file-parallelism`: PASS, 20 files / 110 tests.
- `npx tsc -b --pretty false`: PASS, exit 0.
- `npm run audit:ui`: PASS, `audit:ui clean — 83 files scanned`.
- `npm run build`: PASS. Vite transformed 117 modules; the existing bundle-size warning remains.
- `npx vitest run --config vitest.integration.config.ts src/itest/professor.itest.tsx src/itest/professor-seats.itest.tsx src/itest/professor-fixes.itest.tsx`: PASS, 3 files / 13 tests, 25.60s. The known Firebase Admin metadata lookup warnings were non-fatal.
- `npx vitest run --config vitest.integration.config.ts src/itest/finale.itest.tsx`: PASS, 1 file / 3 tests, including the professor reveal stepper and full drive to FINALE.
- Fresh HTTP-callable Done/revision scenario: PASS. Game `E4BJPsOnjA9B6Eo2bMDj` / join code `E4BJPS` marked Revision Alpha done in FREE_AGENCY, then successfully signed pid 1067 while the same done stamp remained.
- `git diff --check` and staged whitespace/ownership inspection: PASS before the implementation commit.

A supplemental attempt to run `backend/functions/test/done.test.js` directly was unavailable because this prepared screen worktree has no backend `vitest` dependency installed. `npx` could not load the backend config and no backend test executed. No manifest or lockfile changed. The same required Done-is-revisable behavior passed through the fresh HTTP-callable scenario above.

## Browser and layout evidence

Vite used the assigned command `npx vite --host 127.0.0.1 --port 5191 --strictPort`. Browser mutations ran sequentially against the reserved `salary-showdown-dev` emulators; services were not restarted, reconfigured, or cleared.

- Fresh browser game: `UlHdOWtMx5SgEMco6rwZ` / join code `ULHDOW`.
- Inspected the initial 0-team state, then 21 late-created franchises. All 21 cards and per-team seat buttons remained reachable; only GM seats were claimed, so missing Scout/Coach roles were visible on every card.
- Started the 21-team season and inspected Draft Night with 0/21 submitted, the active phase control, auto-armed timer, then the paused timer state.
- Seat tools showed all 21 franchises in the selector. Open moved focus to Close; closing restored focus to the invoking Manage seats button.
- Exact CSS viewports 1280×720, 1366×768, 1440×900, and 1024×720 each reported `documentElement.scrollWidth === innerWidth`.
- At 1280×720 and `scrollY = 1440`, the sticky control group remained at `top = 14px`, alongside the scrolling readiness board, with no page-level horizontal overflow.
- Temporary browser tabs were closed, viewport overrides reset, and the W09 Vite process stopped.

Screenshot paths: none. Inline captures were inspected, but the user deferred saved screenshots and the full integrated visual review until I01 acceptance; no persisted screenshot artifact is claimed.

## Pending checks and limitations

- Per the live I01 ledger, the full integrated visual review, persisted before/after screenshots, actual 200% browser zoom, a real reduced-motion preference toggle, projector/classroom review, and final 21-team representative three-role drill remain pending final I01 acceptance. The viewport checks above are local W09 layout evidence, not a final visual pass.
- Node 20 runtime parity and the coordinator-recorded broader emulator/runtime limitations remain pending I01.
- No deployment or production check ran.

## Interfaces and slot release

Frozen shared interface changes or deviations: **None**. W09 added only task-local component props between `ProfessorPage`, `SubmissionGrid`, and `SeatPanel`; ProfessorContext and every callable payload remain unchanged.

**W09 explicitly releases its exclusive mutable emulator slot.** All W09 mutable checks are finished; coordinator-owned services and shared DEV data remain intact.
