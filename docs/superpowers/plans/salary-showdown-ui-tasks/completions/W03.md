# W03 completion — Front office decisions and payroll timeline

Status: Ready for coordinator review. The exclusive mutable emulator slot is released with this handoff.

## Git state

- Branch: `codex/ss-ui-w03`
- Frozen base: `b48cdf620c594aba0386fa6775af86aea40da401`
- Implementation commit: `96f56bb3ff2e92b3ffd03ac6256e13a2425e4e86`
- Final note-inclusive commit: the commit containing this completion note; the exact SHA is included in the direct I01 report.

## Changed files

- `games/salary-showdown/app/src/pages/FrontOfficePage.tsx`
- `games/salary-showdown/app/src/pages/FrontOfficePage.module.css`
- `games/salary-showdown/app/src/components/frontoffice/CutPreview.tsx`
- `games/salary-showdown/app/src/components/frontoffice/CutPreview.module.css`
- `games/salary-showdown/app/src/components/frontoffice/CutPreview.test.tsx`
- `games/salary-showdown/app/src/components/frontoffice/FrontOfficePage.test.tsx`
- `games/salary-showdown/app/src/lib/cutPreview.ts`
- `games/salary-showdown/app/src/lib/cutPreview.test.ts`
- `games/salary-showdown/app/src/itest/frontoffice.itest.tsx`
- `docs/superpowers/plans/salary-showdown-ui-tasks/completions/W03.md`

## Delivered behavior

- Replaced the narrow stacked screen with a responsive decision ledger beside a persistent player/contract rail and the five-round payroll timeline.
- Added current-ask renewal terms, covered-round totals, every-round cap validation, and candidate payroll preview without counting the expired contract twice.
- Kept re-signed, walked, and re-signed-then-cut entries visible and understandable while the phase remains open.
- Kept Let walk local and reversible with Undo; it performs no callable and emits no saved receipt.
- Added scoped success receipts for accepted re-signs and cuts. Rejections preserve the selected player and contract length and render actionable error copy.
- Added a factual cut confirmation showing the named player, roster effect, every remaining annual obligation, and projected cash/dead-money timeline. The pure `previewCut` projection preserves input state, existing dead money, spend log, and total remaining payroll.
- Preserved GM ownership/fallback behavior, Done-as-status behavior, synthetic hardship exclusion, raw-data-only display, and read-only auction preview boundaries.

## Verification evidence

- `npm test` — PASS: 23 files / 123 tests.
- `npm test -- src/lib/cutPreview.test.ts` — PASS: 1 file / 6 tests.
- `npm test -- src/components/frontoffice/CutPreview.test.tsx` — PASS: 1 file / 3 tests.
- `npm test -- src/components/frontoffice/FrontOfficePage.test.tsx` — PASS: 1 file / 4 tests.
- `npx vitest run --config vitest.integration.config.ts src/itest/frontoffice.itest.tsx` — final PASS: 1 file / 2 tests. Fresh DEV games were created by the harness; it does not print their IDs. The run emitted only the existing non-fatal Firebase Admin `MetadataLookupWarning`.
- The first expanded integration run failed 1/2 because the live team snapshot removed the cut contract before the pending modal closed. A focused regression reproduced that race; `CutPreview` now stays mounted on the already-applied server snapshot. Two subsequent fresh-game integration runs passed 2/2 without retry/config/timeout changes.
- `npx tsc -b --pretty false` — PASS.
- `npm run audit:ui` — PASS: 85 files scanned.
- `npm run build` — PASS: 124 modules transformed. Existing advisory remains: the main minified chunk is larger than 500 kB.
- `git diff --check` and staged whitespace check — PASS.

## Browser and manual checks

- Vite started successfully on the assigned `127.0.0.1:5185` strict port.
- Fresh seeded DEV game for the local preview: `b9tt9ptQaxSS1skusZyA`, join code `B9TT9P`, at `R2:FRONT_OFFICE`.
- The app loaded in the browser through the join screen. No extra browser membership was created because the user deferred the full integrated visual pass and screenshot capture to I01.
- Screenshot paths: None.

Pending for final I01 acceptance, per the live ledger/user deferral:

- Full integrated visual/classroom review after all screens merge.
- Saved screenshot evidence at 1280×720, 1366×768, 1440×900, and the 1024px fallback.
- Actual 200% browser zoom reflow.
- Real reduced-motion preference toggles.
- Live manual round-five and empty-expiring visual scenarios; their data/DOM behavior is covered by unit tests, including the round-five payroll horizon and last-round cut obligation.

## Contracts and deviations

- Produced the planned `previewCut(team, pid, round): TeamDoc` interface.
- Shared foundation, backend, manifests, lockfiles, routing, and emulator configuration were unchanged.
- Interface deviations: None.
- Emulator handling: used only the granted `salary-showdown-dev` slot, ran mutable checks sequentially with fresh games, preserved the browser-SDK alias, and did not restart/reconfigure services or clear data. Slot released.
