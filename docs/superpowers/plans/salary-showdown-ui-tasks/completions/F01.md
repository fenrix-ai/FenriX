# F01 completion note

## Commits

- Branch: `codex/ss-ui-f01`
- Accepted bootstrap/base: `2156f3a11c305600a2e58e0b4e483615d10081f2`
- Implementation HEAD: `13d61c72c48ed69afd3756405ed5c14e3e510d93`
- Final note-inclusive HEAD: resolve from `git rev-parse HEAD` after this note commit; the worker response reports the immutable value.

## Changed files

- `games/salary-showdown/app/src/lib/franchiseIdentity.ts`
- `games/salary-showdown/app/src/lib/franchiseIdentity.test.ts`
- `games/salary-showdown/app/src/lib/payrollProjection.ts`
- `games/salary-showdown/app/src/lib/payrollProjection.test.ts`
- `games/salary-showdown/app/src/types/models.ts`
- `games/salary-showdown/app/src/components/franchise/FranchiseMark.tsx`
- `games/salary-showdown/app/src/components/franchise/FranchiseMark.module.css`
- `games/salary-showdown/app/src/components/players/PlayerCard.tsx`
- `games/salary-showdown/app/src/components/players/PlayerCard.module.css`
- `games/salary-showdown/app/src/components/contracts/PayrollTimeline.tsx`
- `games/salary-showdown/app/src/components/contracts/PayrollTimeline.module.css`
- `games/salary-showdown/app/src/hooks/useReducedMotion.ts`
- `games/salary-showdown/app/src/hooks/useActionReceipt.ts`
- `games/salary-showdown/app/src/hooks/useActionReceipt.test.tsx`
- `games/salary-showdown/app/src/styles/experience.css`
- `games/salary-showdown/app/src/main.tsx`
- This completion note.

`games/salary-showdown/app/src/styles/arena.css` was reviewed but did not require a change. No files owned by another task were edited.

## Implementation summary

- Added the frozen `TeamIdentity`, deterministic legacy fallback, and monogram contracts; `TeamDoc.identity` is optional so old games need no migration.
- Added decorative franchise SVG marks with six CSS palette pairs and three jersey treatments.
- Added a raw-data-only player card with position, glyph hype, salary, missing-value fallbacks, and a caller-owned action area.
- Added five-round payroll projection and a labelled cash/dead/candidate timeline. Candidate salary applies only to covered rounds, old expired contracts are not double-counted, and every over-cap round remains visible as a warning rather than disabling an action.
- Added scope-safe action receipts and a live reduced-motion media-query subscription with cleanup and an SSR/test fallback.
- Added shared `--ss-*` color, spacing, and motion tokens plus the responsive `.ss-workspace` utility and visible focus treatment. Imported the stylesheet once from `main.tsx`.

## Test evidence

Commands ran from `games/salary-showdown/app`.

- Baseline `npm test`: PASS, 15 files / 81 tests.
- TDD red run for the three new test files: expected failure, 10 assertions failed against placeholder implementations after module existence was established.
- `npm test -- src/lib/franchiseIdentity.test.ts src/lib/payrollProjection.test.ts src/hooks/useActionReceipt.test.tsx`: PASS, 3 files / 14 tests.
- Final `npm test`: PASS, 18 files / 95 tests.
- `npx tsc -b --pretty false`: PASS, exit 0.
- `npm run audit:ui`: PASS, `audit:ui clean — 77 files scanned`.
- `git diff --check`: PASS before the implementation commit.
- Dev server command: `npx vite --host 127.0.0.1 --port 5180 --strictPort`; Vite 8.1.5 served the local static component acceptance fixture successfully. The fixture was removed before commit.

No emulator-backed suite, seed, or browser mutation ran. F02 retained the first worker emulator slot throughout.

## Browser and accessibility evidence

- Static, non-emulator component inspection covered 1280×720, 1366×768, 1440×900, and 1024×720. Each viewport reported `documentElement.scrollWidth === innerWidth`.
- At 1024px the workspace collapsed to one column. A 512px CSS viewport (the 1024px-at-200%-zoom reflow equivalent) retained page width at 512px and confined the five-round overflow to the labelled timeline scroller (`446px` client width / `712px` scroll width).
- Long player names wrapped without page overflow; missing salary/stat values rendered as an em dash; a zero-payroll timeline rendered `$0.0M`; a round-4/5 candidate preview displayed both over-cap warnings.
- Keyboard focus on the action button measured a solid 3px `rgb(255, 201, 77)` outline.
- The six primary/ink palette contrast ratios were gold 12.44:1, teal 11.06:1, coral 8.08:1, violet 8.85:1, sky 9.99:1, and mint 11.25:1.
- Reduced-motion preference changes and listener cleanup are covered by the hook test. The stylesheet reduces all new shared motion tokens to `0ms` under `prefers-reduced-motion: reduce`.

Screenshot paths: none. Browser screenshots were captured and inspected inline, but the browser security sandbox blocked exporting those captures to local filesystem paths. No screenshot path is claimed.

## Pending checks and limitations

- Legacy routed pages were not opened because loading the live app can create anonymous-auth/emulator state and F01 did not have the coordinator's emulator-backed browser grant. This remains a coordinator-granted browser smoke check.
- No actual browser-level reduced-motion preference toggle was available; automated matchMedia change/cleanup coverage and CSS media-rule inspection passed.
- Filesystem screenshot persistence is unavailable under the browser sandbox, as noted above.
- No deployment or production check ran.

## Interface changes

- Added `TeamDoc.identity?: TeamIdentity`.
- Added `TeamIdentity`, `resolveIdentity(teamId, identity?)`, and `teamMonogram(name)`.
- Added `PayrollPoint` and `payrollProjection(team, preview?)`.
- Added `FranchiseMark`, `PlayerCard`, and `PayrollTimeline` with the frozen prop signatures.
- Added `useReducedMotion()` and `useActionReceipt(scope)` with `ActionReceipt`.
- Added shared `--ss-*` tokens and `.ss-workspace`.

Interface deviations from the frozen F01 contracts: None.

## Accessibility acceptance correction (2026-09-21)

- Correction branch: `codex/ss-ui-f01-a11y`
- Integrated acceptance base: `88d06ee3e56a46f4b805fb359363e9d8fbe9c106`
- Correction implementation commit: `01a97ee69c31b75369a2049cc97b1aed1fc5d746`
- Final correction note-inclusive HEAD: resolve from `git rev-parse HEAD` after this note commit; the worker response reports the immutable value.

### Scope and outcome

- Changed `games/salary-showdown/app/src/styles/arena.css` so table headers use the existing muted text token instead of the dim token. Against the audited `#091524` table background, the resulting `#9fb4d0` foreground computes to 8.66:1 (the integrated acceptance evidence measured the prior combination at 3.82:1).
- Changed `games/salary-showdown/app/src/components/contracts/PayrollTimeline.tsx` so the horizontal five-round `<ol>` is keyboard-focusable and receives its accessible name from the existing `Five-round payroll` heading.
- No gameplay rules, data flow, public TypeScript interfaces, or frozen F01 contracts changed.

### Verification

Commands ran from `games/salary-showdown/app` without starting or restarting an app server or emulator.

- Focused regression red: the real rendered payroll list could not be found by the accessible name `Five-round payroll` and had no `tabindex`.
- Focused regression green after the fix: PASS, 1 file / 1 test. The temporary regression file was removed before commit to preserve the coordinator's exclusive ownership boundary.
- `npm test`: PASS, 38 files / 213 tests.
- `npx tsc -b --pretty false`: PASS, exit 0.
- `npm run audit:ui`: PASS, `audit:ui clean — 122 files scanned`.
- `git diff --check`: PASS before the implementation commit.

Acceptance evidence reviewed:

- `/Users/dylanmassaro/FenriX/.worktrees/ss-ui-evidence/2026-09-21/acceptance-teams2.json`
- `/Users/dylanmassaro/FenriX/.worktrees/ss-ui-evidence/2026-09-21/screenshots/teams2-results-1280x720.png`
- `/Users/dylanmassaro/FenriX/.worktrees/ss-ui-evidence/2026-09-21/screenshots/teams2-front-office-1280x720.png`

Pending: coordinator-owned browser/axe rerun on the integrated two-team scenario. No emulator-backed suite, browser mutation, deploy, or session creation ran in this correction worktree.

### Shared dim-text contrast follow-up

- Updated only the global `--dim` token in `games/salary-showdown/app/src/styles/arena.css`, from `#5f7396` to `#91a6c5`. This covers essential secondary text that axe did not consistently evaluate over gradients, including rank/page labels, paused-timer copy, role fallbacks, loading states, and error detail.
- WCAG contrast calculations for `#91a6c5`: 5.11:1 against the light card endpoint (`#1a3357`; the stylesheet's `#1b3357` also yields 5.11:1), 6.13:1 against the dark card endpoint (`#142640`), 5.81:1 against the light page endpoint (`#132a4a`), and 6.98:1 against the dark page endpoint (`#0d1b2e`). All exceed the 4.5:1 normal-text requirement.
- The hierarchy remains intact beneath `--muted: #9fb4d0`; muted text measures 5.99:1, 7.18:1, 6.80:1, and 8.17:1 against the same evidence-card, dark-card, light-page, and dark-page endpoints respectively.
- Focused numerical WCAG luminance/contrast verification passed across the evidence card, stylesheet card, and both page-gradient endpoints; it also verified that `--dim` remains lower-emphasis than `--muted` on every surface. No unit test merely asserts color strings.
- `npx tsc -b --pretty false`: PASS, exit 0. `npm run audit:ui`: PASS, `audit:ui clean — 122 files scanned`. `git diff --check`: PASS before commit.
- No API, gameplay rule, feature behavior, emulator/browser state, server, deployment, or session changed.
