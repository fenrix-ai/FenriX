# W10 completion — projector sizing and broadcast pacing

Status: the 2026-09-21 acceptance correction is implemented and locally verified. Coordinator MAIN owns the fresh browser recapture, so the corrected visual cases remain pending until that measured matrix completes. The historical implementation evidence follows the acceptance-correction record.

## Acceptance correction — 2026-09-21

- Coordinator: MAIN task `01a0ac95-cdb0-7963-98b2-fe533310d212`.
- Durable branch: `codex/ss-ui-w10-acceptance`.
- Durable worktree: `/Users/dylanmassaro/FenriX/.worktrees/ss-ui-w10-acceptance`.
- Exact accepted integration base: `88d06ee3e56a46f4b805fb359363e9d8fbe9c106`.
- Initial correction commit merged for recapture: `fac8d5f17fe09fed0c5d76d0dc69e39025d5a1cf`.
- Final scoped follow-up and note-inclusive HEAD: the commit containing this section; the direct Coordinator MAIN report resolves its immutable SHA.

### Acceptance defects and corrections

Coordinator evidence showed four W10-owned layout failures:

1. Finale chart steps 1–4 had document heights of 781px at 1280×720 and 1120px at 1920×1080. Width-driven intrinsic SVG height plus a vertically centered shell clipped chart axes/labels and, on some steps, header content.
2. The 21-team lobby measured 758px at 1280×720, clipping the bottom Coach seats.
3. Dense lobby headings forced every franchise name onto one ellipsized line beside a 44px mark, making names such as `Classroom Franchise 03` indistinguishable and hiding the ends of the two 24-character names.
4. Reduced-motion 21-team RESULTS rendered all placements in a 3×7 grid and measured 839px at 1280×720. The complete immediate reveal is required, so pagination was not substituted for the reduced-motion contract.

The correction:

- Adds a finale-specific viewport shell. Steps 1–4 allocate the remaining vertical space to a flex chart wrapper and fit each existing shared SVG on both axes; no shared chart internals or props changed. Step 0 remains centered. At CSS widths up to 700px, finale returns to auto height and scrollable reflow so actual 200% zoom is not trapped by the fixed projector shell.
- Compacts only the dense lobby masthead at short projector heights, reserves three equal grid rows, uses 32px dense franchise marks, and permits full multi-line franchise names. The previous 13px name and 10px role-chip minimums remain intact, and all 21 names plus all 63 role chips remain in the rendered wall.
- Adds a results-specific viewport shell and a compact 7×3 reduced-motion classroom grid, with five-column fallback and one-column zoom reflow. The classroom treatment is scoped to more than 12 rows; small-league reduced standings retain their original readable three-column wall.

Only these owned files changed in the acceptance correction:

- `games/salary-showdown/app/src/components/bigscreen/FinaleWall.tsx`
- `games/salary-showdown/app/src/components/bigscreen/LobbyWall.tsx`
- `games/salary-showdown/app/src/components/bigscreen/StandingsShuffle.tsx`
- `games/salary-showdown/app/src/components/bigscreen/bigscreen.test.tsx`
- `games/salary-showdown/app/src/styles/bigscreen.css`
- This completion note.

No dependency, lockfile, shared chart, API, backend, emulator, or deployment state changed.

### Acceptance-correction verification

Commands ran from `games/salary-showdown/app`.

- Baseline `npm test -- --maxWorkers=1`: PASS, 38 files / 213 tests.
- Initial TDD red: focused W10 suite failed 7 expected assertions for the missing finale/results layout scopes and dense 32px marks.
- Review-follow-up TDD red: focused suite failed the one expected missing `is-classroom-grid` assertion while the new two-team preservation assertion already passed.
- `npx vitest run src/components/bigscreen/bigscreen.test.tsx --maxWorkers=1`: PASS, 1 file / 14 tests.
- `npm test -- --maxWorkers=1`: PASS, 38 files / 215 tests.
- `npm run build`: PASS, TypeScript plus Vite build, 161 modules transformed. The existing advisory for a chunk over 500kB remains non-fatal.
- `npm run audit:ui`: PASS, 122 files scanned.
- `git diff --check`: PASS before both correction commits.

Coordinator MAIN retained the mutable emulator/browser slot. This worker did not seed, mutate, clear, reconfigure, or restart the shared services and did not run a competing browser session.

### Acceptance visual evidence and remaining check

Failure evidence was inspected directly under `/Users/dylanmassaro/FenriX/.worktrees/ss-ui-evidence/2026-09-21/screenshots`, including:

- `/Users/dylanmassaro/FenriX/.worktrees/ss-ui-evidence/2026-09-21/screenshots/teams21-projector-lobby-1280x720.png`
- `/Users/dylanmassaro/FenriX/.worktrees/ss-ui-evidence/2026-09-21/screenshots/teams21-projector-results-1280x720.png`
- `/Users/dylanmassaro/FenriX/.worktrees/ss-ui-evidence/2026-09-21/screenshots/teams2-projector-finale-1-1280x720.png`
- `/Users/dylanmassaro/FenriX/.worktrees/ss-ui-evidence/2026-09-21/screenshots/teams2-projector-finale-2-1280x720.png`
- Corresponding finale steps 1–4 at 1920×1080 and 3840×2160.

Fresh corrected screenshots and measured document heights at 1280×720, 1920×1080, 3840×2160, and actual 200% zoom remain pending Coordinator MAIN recapture. This note does not claim those browser cases pass before that evidence arrives.

Acceptance-correction interface additions or deviations: **None**.

### Supplemental reduced-motion SIMULATE correction — 2026-09-21

Coordinator MAIN's completed supplemental matrix found the one projector mode missing from the original captures: a real 21-team SIMULATE wall with `prefers-reduced-motion: reduce`. Because the reduced branch rendered every live-standing row in the narrow sidebar, it measured 1229px at 1280×720 and 1500px at 1920×1080, clipping essential ranks and the round-complete status. Completed scorecards also ellipsized the shared `Classroom Franchise …` prefixes, making matchups difficult to distinguish.

Failure evidence inspected directly:

- `/Users/dylanmassaro/FenriX/.worktrees/ss-ui-evidence/2026-09-21/screenshots/teams21-supplement-projector-simulate-reduced-1280x720.png`
- `/Users/dylanmassaro/FenriX/.worktrees/ss-ui-evidence/2026-09-21/acceptance-teams21-supplement.json`

The supplemental correction is deliberately scoped to reduced-motion simulations with more than 12 standings rows. It keeps all 21 server-ordered ranks immediately rendered, but moves the standings below the eight-score page as a 7×3 compact wall grid. Scorecards use the resulting full wall width and wrap complete team names rather than ellipsizing them. Five-column fallback and one-column ≤700px reflow preserve the existing 200%-zoom vertical-scroll behavior. Default-motion prefix/privacy, eight-score pagination, raw standings order, and smaller-league split walls are unchanged.

Additional TDD and verification evidence:

- Red: the focused suite failed exactly the new missing dense reduced-SIMULATE layout assertion; the paired small-league preservation regression already passed.
- `npx vitest run src/components/bigscreen/bigscreen.test.tsx --maxWorkers=1`: PASS, 1 file / 16 tests.
- `npm test -- --maxWorkers=1`: PASS, 38 files / 217 tests.
- `npm run build`: PASS, TypeScript plus Vite build, 161 modules transformed; existing chunk-size advisory only.
- `npm run audit:ui`: PASS, 122 files scanned.
- `git diff --check`: PASS.

Coordinator MAIN retains the only emulator/browser slot. This worker made no emulator, browser, server, or deployment mutation. A fresh corrected supplemental capture remains pending MAIN and is not claimed passing here.

Supplemental interface additions or deviations: **None**.

## Provenance

- Task/thread: W10, `01a0b2b6-a031-7fd0-9edd-91f21fdae7bc`.
- Branch: `codex/ss-ui-w10`.
- Accepted frozen base: `b48cdf620c594aba0386fa6775af86aea40da401`.
- Implementation commit: `6179fa84c13ae8ce756580b0131c4abc06c0e840`.
- Coordinator-review correction commit: `ef442dd2af22d40296d663be387303816903f5d6`.
- Final note-inclusive HEAD: the commit containing this note; the direct coordinator report and worker response provide its resolved immutable SHA.

Preflight confirmed the assigned branch, exact accepted base, and clean worktree. Work stayed inline in the coordinator-prepared worktree. No subagent, additional worktree, rebase, merge, deployment, emulator restart, emulator reconfiguration, or shared-data clear was used.

## Changed files

- `games/salary-showdown/app/src/components/bigscreen/DecisionWall.tsx`
- `games/salary-showdown/app/src/components/bigscreen/FinaleWall.tsx`
- `games/salary-showdown/app/src/components/bigscreen/LobbyWall.tsx`
- `games/salary-showdown/app/src/components/bigscreen/SimulateFlood.tsx`
- `games/salary-showdown/app/src/components/bigscreen/StandingsShuffle.tsx`
- `games/salary-showdown/app/src/components/bigscreen/bigscreen.test.tsx`
- `games/salary-showdown/app/src/components/bigscreen/useBroadcastProgress.ts`
- `games/salary-showdown/app/src/components/bigscreen/useWallPage.tsx`
- `games/salary-showdown/app/src/itest/bigscreen.itest.tsx`
- `games/salary-showdown/app/src/styles/bigscreen.css`
- This completion note.

The two new hooks and colocated test are tightly local additions under W10's owned `components/bigscreen` directory. No manifest, lockfile, backend, shared type, shared chart implementation, routing, or another task's file changed.

## Implementation summary

- Added projector-local elapsed broadcast progress with the frozen 1200 ms intro and clamped game interval, session-scoped high-water persistence, visibility/background reconciliation, and nondecreasing reveal state. It remains read-only and makes no synchronization claim across devices.
- Added fixed-cadence, no-scroll wall pagination for large classroom rosters and standings, with clear page status and complete row coverage.
- Scaled lobby and decision walls to readable 21-team layouts and used the frozen public franchise identity treatment.
- Reworked simulation presentation around elapsed matchup and score prefixes. Final records and completed totals remain hidden until their server-ordered game prefix; `liveStandings` remains the arithmetic source.
- Preserved round-five top-three suspense and finale reveal-step clamping. Reduced motion completes the local reveal immediately.
- Added explicit responsive finale chart space with `min-width: 0`, projector-scale width/height, narrow/200%-equivalent reflow rules, and reduced-motion overrides without changing shared chart props.
- Hardened `StandingsShuffle` after I01 review: progress is scoped by game ID and round, equivalent round snapshot objects cannot replay a completed shuffle, reduced-motion false cannot reshroud already exposed placements, and retired timers cannot mutate a replacement scope.

All projector views remain read-only. Gameplay rules, server standings order, transition gating, reveal privacy, and shared APIs are unchanged.

## Verification evidence

Commands ran from `games/salary-showdown/app` unless an absolute config path is shown.

- TDD red evidence: the new W10 projector regressions failed against the preimplementation views for the expected missing pacing, pagination, privacy, and sizing behaviors before the source changes.
- `npx vitest run src/components/bigscreen/bigscreen.test.tsx`: PASS after correction, 1 file / 12 tests.
- `npm test -- src/lib/liveStandings.test.ts src/lib/shuffle.test.ts src/components/bigscreen/bigscreen.test.tsx`: PASS, 3 files / 20 tests.
- `npx vitest run --config /private/tmp/salary-showdown-ui-worktrees/evidence/i01-w10-review/vitest.review.config.mjs`: initial review run reproduced 2 failures; after correction PASS, 1 file / 12 tests. The external fixture/config was run unchanged.
- `npm test -- --maxWorkers=1`: PASS, 21 files / 122 tests. A final pre-commit rerun reconfirmed the same result in 9.96 seconds. One attempted invocation with unsupported `--minWorkers` exited during CLI parsing before test collection; removing that invalid option produced the passing run recorded here.
- `npx tsc -b --pretty false`: PASS, exit 0.
- `npm run audit:ui`: PASS, 83 files scanned.
- `npm run build`: PASS, 117 modules transformed. Vite emitted the existing advisory that one output chunk exceeds 500 kB; it did not fail the build.
- `npx vitest run --config vitest.integration.config.ts src/itest/bigscreen.itest.tsx`: PASS, 1 file / 4 tests in 77.51 seconds against the coordinator-managed DEV emulators. The run emitted the existing Firebase Admin metadata lookup warning and no assertion failure.
- `git diff --check`: PASS before each source commit and before this note.

The emulator integration used the exclusive slot granted by I01. Immediately before the run, Auth9199 and Firestore8180 returned HTTP 200; Functions5101 was listening and returned the expected HTTP 404 at its root. The harness created fresh DEV games but did not print their generated IDs. The browser-SDK alias/configuration was preserved, no production service was used, and W10 explicitly released the slot to I01 immediately after the passing run.

The integration coverage includes all five finale steps, missing-reveal loading, out-of-range reveal clamping, direct 21-team SIMULATE/RESULTS fixtures, every paginated row, and no final total before its ordered prefix.

## Visual evidence and pending acceptance

Screenshot paths: **none**.

Per the session prompt, the full integrated visual/classroom review, saved screenshots, actual browser 200% zoom, and a real browser/OS reduced-motion preference toggle are deferred to final I01 acceptance. Exact browser inspection at 1280×720, 1920×1080, and 3840×2160 with two and 21 teams, long-name review, no-mouse viewing, and projector screenshot capture therefore remain pending and are not claimed as passing here. The automated component/integration coverage and static CSS inspection exercise the corresponding data volume, paging, reveal, reduced-motion, and reflow logic, but do not replace that final manual evidence.

## Interfaces

Interface additions or deviations from the frozen contracts: **None**.

W10 consumes the existing franchise identity, reduced-motion, presentation, standings, and chart contracts without changing their required props or behavior.

## Emulator-slot release

W10 explicitly released the exclusive `salary-showdown-dev` mutable emulator slot after the passing projector integration run. The coordinator-owned processes remain running and shared DEV data was preserved.
