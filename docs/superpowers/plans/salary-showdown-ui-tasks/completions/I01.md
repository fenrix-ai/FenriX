# I01 coordination ledger

Status: F01–F03 and W01–W10 accepted and merged; production deployed 2026-09-18 by explicit user request. Automated tests/types/build/audit PASS. Visual/classroom acceptance and local Node20 runtime parity remain pending; all workers idle. Original shared base remains frozen.



## Acceptance continuation — 2026-09-21

- Current coordinator: Coordinator MAIN, at the user's request to complete the pending checks. No production mutation/deployment is authorized or performed in this continuation.
- Recovered I01 from temporary cleanup: 685 tracked files were missing and the worktree link was gone. Preserved surviving files, restored only missing tracked files from `88d06ee3e56a46f4b805fb359363e9d8fbe9c106`, and repaired registration at `/Users/dylanmassaro/FenriX/.worktrees/ss-ui-integration`. Product diff was clean after recovery; original checkout remains untouched.
- Evidence/tools are now under durable ignored directories `/Users/dylanmassaro/FenriX/.worktrees/ss-ui-evidence/2026-09-21` and `/Users/dylanmassaro/FenriX/.worktrees/ss-ui-tools`.
- Coordinator holds the exclusive local mutable emulator/browser slot while this continuation runs. Existing idle data was exported through the verified hub endpoint (CLI discovery was missing), then reimported into the restored backend service. Project remains `salary-showdown-dev`, ports unchanged. Node 20.20.2 runtime confirmed by Firebase's startup log; Vite runs on reserved 5193.
- Node 20 frontend suite: 38 files / 213 tests PASS; TypeScript and UI audit (122 files) PASS. Backend and browser-SDK runtime checks remain pending in this continuation.
- Browser acceptance uses isolated Chromium test profiles, real 200% tab zoom (1280 physical width → 640 CSS pixels, devicePixelRatio 2), saved screenshots and axe scans. Complete results and honest limitations will follow; no visual/classroom pass is claimed while the matrix is in progress.


## Production deployment — 2026-09-18

This section supersedes the older undeployed/preview status below. The user explicitly requested “deploy it for real” after reviewing the preview and the required additive function deployment.

- Live: https://salary-showdown.web.app/ (professor panel: https://salary-showdown.web.app/professor).
- Deployed from clean integration commit `9d1ff3e62dc5764f2434648844afe05efbfaa20e`, branch `codex/salary-showdown-ui-integration`, in `/private/tmp/salary-showdown-ui-worktrees/I01`. Fresh production build PASS; ignored production web configuration only. Main checkout untouched.
- Additive `setTeamIdentity` callable successfully created in `salary-showdown`, us-west1, Node 20, before Hosting deployment. Unauthenticated callable smoke check correctly returned HTTP 401 / UNAUTHENTICATED. No other functions or Firestore rules deployed; no production game fixtures created or modified.
- Hosting release `1789769610242000`, version `f8171fbd999bbfc1`, released at `2026-09-18T22:13:30.242Z`. Live HTML HTTP 200; JS `index-C8qFzVo4.js` and CSS `index-CYTQUwbi.css` SHA-256 match the freshly built local assets. Browser visibly renders the new three-step join page; captured warning/error console is empty.
- Rollback target: previous live version `7d5ce13d20e4ed83`, release `1789597570840000` (2026-09-16T22:26:10.840Z), recorded before deployment. Restore through Firebase Hosting release history if needed; the backward-compatible additive callable can remain.
- Evidence outside git: `/private/tmp/salary-showdown-ui-worktrees/evidence/production-release-build.log`, `production-identity-deploy.log`, `production-hosting-deploy.json`, `production-channels-before.json`, `production-channels-after.json`.
- Automated integration evidence remains 213 frontend unit, 53 browser-SDK integration, and 214 backend tests passing, plus types/build/UI audit. Full saved visual/classroom matrix, actual zoom/reduced-motion and representative 21-team/three-role/reconnect review remain pending; this deployment does not mark those checks passed. Local Node 20 runtime parity remains unverified. Firebase warned Node 20 is deprecated and will be decommissioned 2026-10-30; runtime/dependency upgrades were not included in this release.

## User-requested deployed preview — 2026-09-18

- User explicitly requested: “let me see a preview, deploy it and give me the link”. Published Firebase Hosting preview channel `ui-preview` in project `salary-showdown` from integrated commit `c4dfae58c6fee119610dddeb6b34fe44119a759f`. Live Hosting release unchanged; full visual/classroom release acceptance remains pending.
- Preview: https://salary-showdown--ui-preview-b03upheg.web.app — expires 2026-09-25T07:24:25.710854396Z. CLI reported success; deployed HTML/assets returned HTTP200 and asset hashes match the production-mode build. Browser showed the new three-step Join your league page; no captured console warnings/errors. Preview tab retained visibly for the user.
- Production web configuration was retrieved through Firebase CLI and written only to ignored `app/.env.production`. Build passes (161 modules; existing bundle-size warning). Published bundle `index-C8qFzVo4.js`; CSS `index-CYTQUwbi.css`. Preview uses the existing real `salary-showdown` backend, not local emulators. No live games were created or modified for this smoke check.
- `functions:list` confirmed the existing fifteen production callables and missing `setTeamIdentity`. Automatic approval review REJECTED `firebase deploy --only functions:setTeamIdentity --project salary-showdown --non-interactive`: persistent production-function mutation exceeds a preview-only request, with hosting-only preview available as safer alternative. No retry/workaround or function mutation was performed. The hosting preview was completed independently. Color/jersey saving remains unavailable until the user explicitly authorizes the additive production function deployment.
- Evidence: `evidence/preview-hosting-deploy.json`, `preview-production-build.log`, `preview-functions.json`, `preview-sdkconfig.json` outside git. These are preview-deployment results, not full classroom validation. User confirmation for the production callable is requested only after publishing the reviewable preview.

## Direct worker completion handoff — user instruction, 2026-09-17

Before ending any implementation or correction run, directly notify the I01 coordinator task "Coordinate Salary Showdown UI" (threadId 01a0acb1-2571-7583-b60e-3df42fa600a1, hostId local) using mcp__codex_app__send_message_to_thread. Include task ID, status (ready for review or blocked/pending), branch and final note-inclusive commit SHA, completion-note path, test results, remaining checks/blockers, and explicit emulator-slot release or retention. This user-authorized handoff is required; a final reply in your own task or a completion file alone is insufficient. Do not ask the user to relay "done". If the messaging tool fails, report the failure honestly and preserve the handoff in your completion note.

New workers must register directly with I01 at startup, including their own task/thread ID for replies, W number, branch, and worktree. They continue independent implementation and send emulator-slot requests/blockers directly to I01. The user authorizes autonomous coordinator follow-ups, review corrections, and integration through these existing tasks. Six remaining startup prompts: `/private/tmp/salary-showdown-ui-worktrees/evidence/remaining-six-session-prompts.md` (W03, W04, W06, W07, W08, W10); W09 is already active and must not be duplicated.

I01 reviews incoming completion reports and continues the established review/integration workflow. This does not grant workers integration authority or change emulator serialization. Existing completed tasks need not resend historical reports; this applies to current and future work.

## Active worker registrations — 2026-09-17

Concurrency reduced at user request: keep at most two implementation workers active. All ten screens are merged; all implementation workers stay idle while I01 runs final acceptance. I01 reviews/integrates separately and serializes all mutable emulator slots. Run I01 full unit and emulator suites separately after the recorded contention timeout; preserve failed-run evidence and unchanged test limits.

| Worker | Task/thread ID (host: local) | Current coordination state |
| --- | --- | --- |
| W03 | `01a0b2b5-ea7e-7433-8033-23907fddf8ad` | Accepted final HEAD `0938a466c5fcc0863198cb682ae3a48d174ccfc6`, merged at `b0c5762e92bc8a43c147b18616050dc143d98e8b`; all review and integrated checks pass with the recorded initial unit timeout retained. Slot released. |
| W04 | `01a0b2b6-0c65-7a50-a111-011c8471222c` | Accepted final HEAD `5302aa1057721a29680350142bbaa8bf1b313e24`, merged at `af3faf165f1e55f3b60e5c0e8a88b124c9c6362b`; integrated auction check PASS at combined W03/W04 head. Worker finished; slot released. |
| W06 | `01a0b2b6-4274-7931-abb8-f086123ae7c3` | Accepted final HEAD `c4b079103945db920d4a2d117b85d8013651b712`; merged at `b842d6b45fddd4cee57bcc537bf40bc78a93660e`. Integrated unit170/types/audit/build and simulate2 PASS; worker and I01 slots released. |
| W07 | `01a0b2b6-5ebb-7aa3-aaf6-e25cecd78815` | Accepted final HEAD `8fda44515c854c57a441d26b516229a1af8541bf`, merged at `aff3eef77673d432b8c68c54bc0226e17c850bba`. Integrated182 unit/types/build/audit and5 emulator tests PASS; worker and I01 slots released. Worker idle. |
| W08 | `01a0b2b6-8251-7350-9baf-9bc177c1e16a` | Accepted final HEAD `21d45236642de7e710c6e575885858de8750d6c2`, merged at `30070923b9393b34570300320c3107fff30cde9d`; coordinator external4/integrated201 unit/types/build/audit and finale3 PASS. Worker idle; slot released. |
| W09 | `01a0b291-2603-7752-b5a2-309d2c7e479d` | Accepted and integrated at `3127f5425a903ce54c940f27450989fbefaceed9`; worker and I01 slots released. |
| W10 | `01a0b2b6-a031-7fd0-9edd-91f21fdae7bc` | Accepted final HEAD `dd8fc5dd3684533db1dc1cf2588a317592c5c35f`, merged at `c586f18dc66ad0e278de605d751401eb3b99e143`. Source/ownership/ancestry/whitespace verified; external12 PASS, integrated213 unit/types/build/audit PASS. Integrated bigscreen passed within full run; unrelated integration selector failures under correction. Worker idle; slot released. |

## Current screen release — 2026-09-17

- User response to the gate choice: “can you show me? or maybe we do visual checks after all of it”. I01 proceeds with full visual review deferred until all screens are integrated. This is a scheduling deferral, not a visual pass. Saved screenshots, actual 200% browser zoom, actual reduced-motion preference toggles, and final classroom review remain required. The recorded emulator timeout and Node runtime parity limitations also remain open for final acceptance; no failed run is reclassified.
- **Exact immutable shared base:** `b48cdf620c594aba0386fa6775af86aea40da401`, branch `codex/salary-showdown-ui-base`. Includes accepted F01, F02, corrected F03, and the reviewed integration-only lobby selector fix. All ten screen branches start at this exact SHA. I01 now advances through accepted screen merges; workers keep this original frozen base unless explicitly instructed otherwise.
- W01–W10 worktrees are prepared below. Each must verify its exact HEAD, branch, and clean status before editing. Dependencies use the existing app lockfile via `npm ci --offline --no-audit --no-fund`; no manifests/locks or production env files changed. No implementation sessions or subagents were created by I01. The user starts the sessions.
- **Current exclusive mutable emulator reservation: none.** I01 completed and released all automated suites. All workers idle. Reserve the next browser/classroom drill explicitly before mutating fresh DEV fixtures; never overlap it with another suite. No deployment, restart, reconfiguration, or data clear.
- **W05 reported-readiness incident, 2026-09-17:** I01 found the original coordinator listeners still present (Firebase PID 98308, Firestore PID 98420). Read-only requests inside I01's restricted sandbox failed with `Operation not permitted`; the same requests using approved `require_escalated` localhost access returned hub4400 HTTP 200, Auth9199 HTTP 200, Firestore8180 HTTP 200, and Functions5101 root HTTP 404 (an HTTP response from the service, not a callable success test). Hub registered all three expected endpoints. This does not establish a service outage; sandbox access is the likely cause. No process restart, import, data clear, configuration change, or mutation was performed. W05 retains its slot and should recheck readiness/run its authorized DEV integration tests through approved localhost access, reporting any remaining actual failure. Coordinator did not take its mutable slot.
- Coordinator preview is finished; no competing mutable suite is running. The visible I01 preview on port 5193 uses the existing completed F03 correction fixture `fehDcIXmJnB0Y3AKiOk6` / `FEHDCI`, claimed its open Scout seat as `Foundation Preview`, and showed round-1 simulation and shared header. It is left open for the user with default viewport. This is a preview, not final visual verification; no screenshot artifact is claimed.
- Copyable ready prompts: `/private/tmp/salary-showdown-ui-worktrees/evidence/f03-corrected/screen-session-prompts.md`. Each points to this LIVE ledger, since the frozen worktrees contain an older ledger copy. Recommended first wave: W02, W05, W01, W09 (3–4 sessions at a time). All ownership and shared API contracts remain frozen; exceptions come back to I01 before affected work continues.

| Task | Branch | Prepared worktree | Vite |
| --- | --- | --- | --- |
| W01 | `codex/ss-ui-w01` | `/private/tmp/salary-showdown-ui-worktrees/W01` | 5183 |
| W02 | `codex/ss-ui-w02` | `/private/tmp/salary-showdown-ui-worktrees/W02` | 5184 |
| W03 | `codex/ss-ui-w03` | `/private/tmp/salary-showdown-ui-worktrees/W03` | 5185 |
| W04 | `codex/ss-ui-w04` | `/private/tmp/salary-showdown-ui-worktrees/W04` | 5186 |
| W05 | `codex/ss-ui-w05` | `/private/tmp/salary-showdown-ui-worktrees/W05` | 5187 |
| W06 | `codex/ss-ui-w06` | `/private/tmp/salary-showdown-ui-worktrees/W06` | 5188 |
| W07 | `codex/ss-ui-w07` | `/private/tmp/salary-showdown-ui-worktrees/W07` | 5189 |
| W08 | `codex/ss-ui-w08` | `/private/tmp/salary-showdown-ui-worktrees/W08` | 5190 |
| W09 | `codex/ss-ui-w09` | `/private/tmp/salary-showdown-ui-worktrees/W09` | 5191 |
| W10 | `codex/ss-ui-w10` | `/private/tmp/salary-showdown-ui-worktrees/W10` | 5192 |

## W08 accepted and integrated — 2026-09-17

- Accepted final HEAD `21d45236642de7e710c6e575885858de8750d6c2`; whole-branch merge `30070923b9393b34570300320c3107fff30cde9d`. Clean worker, original frozen ancestry, all thirteen files owned and whitespace PASS; no backend/dependency/config/shared-context changes. Optional scatter interactions preserve existing default read-only chart calls.
- All four review failures resolved. Finale snapshot/subscription scoped to game/actual UID/team/FINALE with synchronous render suppression, observable errors and retired callback guards. Podium progress/celebration scoped to game/viewer, active confetti suppressed for reduced motion/scope changes with bounded expiry. Coordinator unchanged external fixture **2 files / 4 tests PASS**, 1.04s; `evidence/i01-w08-review/corrected-regression.log`.
- Integrated serial full unit **37 files / 201 tests PASS**, 20.37s; TypeScript/build PASS (159 modules, existing >500kB warning), UI audit **120 files PASS**. Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w08-accepted/` (`unit.log`, `build.log`, `audit.log`).
- Worker reports corrected finale emulator **1 file / 3 tests PASS**, 8.42s, with known non-fatal MetadataLookupWarning. Its initial sandbox access failure is retained in W08 note. Worker explicitly released; W10 now holds the slot. **I01 integrated finale emulator rerun PASS: 1 file / 3 tests**, 8.36s, after W10 explicitly released. Evidence `evidence/i01-w08-accepted/integration.log`; known non-fatal MetadataLookupWarning only. No competing mutable checks occurred.
- W08 is idle with worktree retained. W10 is the last unmerged screen. Full saved visual matrix, actual 200% zoom, real reduced-motion toggle, classroom/privacy/reconnect checks, Node20 parity and final broader verification remain pending. No final acceptance/deployment claimed.

## Final automated verification passed; visual acceptance pending — 2026-09-17

- All-screen product merge `c586f18dc66ad0e278de605d751401eb3b99e143`; integration test commit `dbde850`. Final automated results: frontend **38 files / 213 unit tests**, browser-SDK integration **21 files / 53 tests**, backend **28 files / 214 tests**, TypeScript/build and audit122 all PASS. Full frontend integration and backend ran sequentially. Original selector failures and command-directory error remain recorded below; no timeouts/assertions were weakened.
- A rendered two-team/five-round season reaches server round5/FINALE with two final standings rows. The new rendered experience fixture verifies a saved auction offer survives standings navigation, revealed simulation results survive route changes, and completed results retain all23 box columns. Existing role, privacy, transition, export, lineup-remount and projector tests remain in the full suite.
- Handoff and runbook now distinguish the undeployed UI candidate from historical production status, record pending gates, and describe additive identity-callable-first deployment plus prior Hosting-release rollback after separate authorization. No production query or deployment ran.
- CUA read-only visual sample: existing DEV fixture `fehDcIXmJnB0Y3AKiOk6` / `FEHDCI`, completed simulation, long franchise name. DOM page width matched1280,1366,1440,1024 at requested720/768/900/720 heights. Inline rendered screenshot at1440×900 showed the header, scoreboard, completed matchup and completion state without clipping. Native screenshot captures immediately after viewport overrides were not reliable; use the browser `screenshot` method after state settles. No saved screenshot file claimed. Normal viewport restored and existing tab retained.
- Attempted browser zoom shortcut did not change CSS viewport or device pixel ratio; actual200% zoom remains unverified. User question requesting explicit permission for already-installed Playwright (separate test browser, saved screenshots, required viewport/motion checks) is pending. CUA's documented alternate-tool restriction requires that explicit request. No alternate control method has been used. Initial automatic-review capacity failure recovered on the same read-only retry and is not a remaining block.
- Remaining final gates: full per-screen viewport/screenshots; actual200% zoom and real reduced-motion toggles; keyboard/focus and background/reconnect;21-team representative three-role classroom drill; two/21-team all-phase projector720p/1080p/4K; Node20 runtime parity (current emulator host is24.16.0). Automated browser-SDK tests are not visual acceptance. All workers are idle and I01 released the mutable slot. Candidate is not release-ready.

## All screens merged; final acceptance in progress — 2026-09-17

- W10 final accepted HEAD `dd8fc5dd3684533db1dc1cf2588a317592c5c35f`; only its completion note changed after reviewed correction `ef442dd2af22d40296d663be387303816903f5d6`. Whole-branch merge/all-screen product candidate `c586f18dc66ad0e278de605d751401eb3b99e143`. Eleven owned files, clean worker, frozen-base ancestry, whitespace PASS; earlier SHA typo explicitly corrected in final handoff.
- Integrated full unit **38 files / 213 tests PASS**, 20.82s; TypeScript/build PASS (161 modules, existing >500kB warning), audit **122 files PASS**. Evidence `evidence/i01-w10-accepted/{unit,build,audit}.log`.
- First all-screen browser-SDK integration run **20 files / 52 tests: 49 PASS, 3 FAIL**, 224.23s. Bigscreen, finale and other17 files passed; failures are obsolete integration selectors in contexts/season/transition. Evidence `evidence/i01-final-acceptance/integration-initial.log`. Contexts assumes the removed `.card` seat picker; season/transition use the old `salary for` label rather than current `Salary per round for`. No service restart/data clear or test timeout change.
- I01 narrow integration-test correction scope includes `app/src/itest/contexts.itest.tsx` and `transition.itest.tsx` for accessible selector updates only, alongside its owned season/experience tests. This allocates integration-only verification fixes; no shared API/context or worker product file changes. Preserve membership recovery, transition gate, five-round assertions and timeout limits. Season now exercises two teams and asserts final server round5/standings; new experience test covers saved auction offer after navigation and shared simulation/results progression across routes.
- First targeted command was mistakenly invoked from the repository root and failed before setup because no integration config exists there; evidence `integration-targeted.log`. Correct app-directory rerun **4 files / 7 tests PASS**, 21.35s (`integration-targeted-app.log`), including two-team full season and saved-bid/shared-progress navigation. Post-test-edit TypeScript PASS. Corrected full suite **21 files / 53 tests PASS**, 192.62s (`integration-corrected-full.log`). Then sequential full backend **28 files / 214 tests PASS**, 33.86s (`backend.log`). Known non-fatal MetadataLookupWarning only; no failed assertion in either final run. This command failure is retained, not treated as a product failure or pass.
- Browser inventory first hit automatic approval review model-capacity failure; retry of the same read-only request succeeded. Current CUA supports inline screenshots and exact viewport overrides but has no documented screenshot file-save API. User authorization question for already-installed Playwright with a separate test profile is pending; no alternate browser-control method used. Node runtime remains v24.16.0; only that NVM version found, backend declares20.
- All workers idle. Automated gates are green. Full visual/classroom/saved screenshot/actual 200% zoom/real motion preference/reconnect and Node 20 runtime parity remain open. No final acceptance/deployment claimed.

## W10 corrected checkpoint review — historical, merged above

- Actual source HEAD `ef442dd2af22d40296d663be387303816903f5d6`, clean at review; original frozen ancestry and whitespace PASS. Worker initially reported an unresolvable full SHA sharing the short prefix; I01 verified actual HEAD and requested the final handoff correct that typo. No product change inferred from the reporting error.
- StandingsShuffle now preserves scope-keyed high-water progress with actual game ID, avoids equivalent snapshot/preference-off resets, reveals all for reduced motion, and guards retired timers. Coordinator unchanged combined fixture **12/12 PASS**, 1.08s; `evidence/i01-w10-review/corrected-regression.log`. No additional source blocker found.
- Worker reports authorized bigscreen integration **1 file / 4 tests PASS**, 77.51s, and explicitly released its slot. Final owned completion note and note-inclusive SHA remain pending; no W10 merge/acceptance claimed yet. I01 holds the slot for its post-merge projector verification; integrated W08 finale3 already PASS.

## W10 preliminary source review — historical, corrected and merged above

- Source reviewed during checkpoint commit; clean HEAD observed after verification: `6179fa84c13ae8ce756580b0131c4abc06c0e840`. Worker reports focused10/packet20/full120 unit, types/audit83/build PASS. All observed edits are owned; no source changes by I01. Stable ancestry/final-note review remains for final submission.
- Coordinator copied the ten owned component tests into an external fixture and added two regressions: **10 existing PASS / 2 new FAIL**, 1.25s. Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w10-review/` (`bigscreen-review.test.tsx`, `vitest.review.config.mjs`, `regression.log`). Command from W10 app: `npx vitest run --config /private/tmp/salary-showdown-ui-worktrees/evidence/i01-w10-review/vitest.review.config.mjs`. No emulator/browser mutations by I01.
- **StandingsShuffle resets already revealed placements:** reduced motion initially exposes both teams, but disabling the preference makes both rows disappear behind shrouds. An equivalent new round snapshot object after the completed reveal causes the same replay. Its effect resets shown on reduced/steps identity changes. This carried-forward pacing pattern is an uncovered acceptance gap. Preserve a nondecreasing reveal high-water mark within the actual game/round presentation scope, reveal all when reduced motion is enabled, and avoid replay on preference-off or equivalent snapshots. Keep championship pacing and paging behavior; use game ID in scope and prevent retired timers from applying to a new scope. Add owned regressions and rerun the unchanged external fixture.
- W10 remains queued NEXT for the bigscreen emulator suite after W08's explicit release and a separate I01 grant. Corrections can proceed locally while waiting; do not claim pending emulator or visual checks as passed.

## W08 initial source review — historical, corrected and merged above

- Clean source checkpoint `a85f8b787004dc7ffb08693e8825e441999fd842`, exact frozen-base ancestry, twelve owned files and whitespace PASS. Worker reports targeted28/full122 unit/types/audit85/build PASS. Finale emulator slot granted; completion note still pending. Existing chart required props preserved with optional scatter interactions; classification computed from the full population before filtering.
- Coordinator external regression fixture **2 files / 4 tests FAIL**, 991ms, all at intended assertions. Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w08-review/` (`finale-scope-review.test.tsx`, `podium-review.test.tsx`, `vitest.review.config.mjs`, `regression.log`). Run from W08 app: `npx vitest run --config /private/tmp/salary-showdown-ui-worktrees/evidence/i01-w08-review/vitest.review.config.mjs`. No emulator/browser mutations by I01.
- **Reveal snapshot scope (3 cases):** switching game retains the prior reveal until a new callback; switching actual auth UID retains the prior viewer data without a new subscription; a retired game callback can overwrite the new game reveal. The carried-forward listener pattern is an uncovered acceptance gap, not claimed newly introduced. Scope local snapshot rendering and subscription by game/actual UID/authorized membership/FINALE, hide stale data synchronously, ignore retired callbacks and keep errors observable while clearing unusable data. Add owned page regressions under `components/finale/` including phase exit and delayed callbacks; preserve independent laptop browsing and shared contexts.
- **Active celebration reduced-motion toggle (1 case):** after Skip starts 28 particles, enabling reduced motion leaves them mounted. Effect cleanup cancels their removal timer, while the existing sessionStorage marker makes subsequent effect runs return before clearing state; toggling motion off can replay mounted CSS animation. Immediately stop/unmount celebration for reduced motion and scope changes, preserve revealed podium progress and the at-most-once game/viewer guarantee. Add owned coverage for preference toggles during active confetti, expiry and scope changes.
- W08 corrects within ownership; W10 continues independently. Existing W08 slot remains exclusive until explicit release; preserve any in-flight original verification result and rerun corrected integration after local fixes while holding its grant, or request a new grant if already released. No final acceptance or visual pass claimed.

## W07 accepted and integrated — 2026-09-17

- Accepted final HEAD `8fda44515c854c57a441d26b516229a1af8541bf`, corrected source/test HEAD `2bd72afeb759ff89b25823fbfa4ed92dc232d02b`; whole-branch merge `aff3eef77673d432b8c68c54bc0226e17c850bba`. Clean worker, frozen-base ancestry, all fourteen files owned, whitespace PASS, no shared/backend/dependency/config changes. The two new scope regressions are now correctly inside `components/results/`.
- All three reproduced review findings resolved: ambiguous duplicate-name/player award stats omitted while authoritative facts remain, auction snapshot rendering scoped by game/round/team/actual UID with retired callbacks ignored, and historical standings local cache includes actual UID. Coordinator unchanged external fixture3/3 PASS before final note/test-path-only changes. Worker final targeted5/full122 unit plus correction emulator5 PASS; explicit slot release received directly.
- Integrated sequential full unit **36 files / 182 tests PASS**, 19.84s; TypeScript/build PASS (155 modules, existing >500kB warning), UI audit **116 files PASS**. Then integrated results/standings/auction-results browser-SDK emulator suite **3 files / 5 tests PASS**, 15.67s, fresh DEV games and existing non-fatal MetadataLookupWarning only. No competing I01 suites, restart, configuration change, or data clear.
- Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w07-accepted/` (`unit.log`, `build.log`, `audit.log`, `integration.log`). Worker and I01 mutable slots released; W07 stays idle with worktree retained.
- Two screens remain: W08 finale/charts and resumed W10 projector. At most two implementation workers remain active. Full saved visual matrix, actual 200% zoom, real reduced-motion toggle, two/21-team classroom/privacy/reconnect checks, Node20 parity and final broader verification remain pending. No final acceptance or deployment claimed.

## W06 accepted and integrated — 2026-09-17

- Accepted final HEAD `c4b079103945db920d4a2d117b85d8013651b712`, unchanged reviewed implementation `d2f03cc52dde5221f06f06e4fa6b65453fe3a3f4`; only its completion note was added after preliminary review. Whole-branch merge `b842d6b45fddd4cee57bcc537bf40bc78a93660e`. Clean worker, exact frozen ancestry, ten owned files, no shared/backend/dependency/config changes, and whitespace PASS.
- Previously reviewed prefix-gated simulation and all-23-column game_id-based expansion have no source blocker. Coordinator focused page/helper tests passed 4/4 before final submission; worker targeted W06/liveStandings suite 9/9 and full114 unit suite PASS. Worker restricted-sandbox integration failed at auth setup before mutations; approved-access rerun unchanged PASS2/2. Both attempts retained in its note. Worker fixtures: `rYNXTt147dmocq9QQfsq` / `RYNXTT` and `K34oSOKgk1x1BcqqPPKk` / `K34OSO`.
- Integrated serial unit suite **33 files / 170 tests PASS**, 17.94s. Then TypeScript/build PASS (150 modules, existing >500kB warning), UI audit **111 files PASS**. After those completed, sequential integrated simulate browser-SDK suite **1 file / 2 tests PASS**, 6.14s, fresh DEV games and known non-fatal Firebase Admin MetadataLookupWarning only. No concurrent I01 full suites, failures, service restart/reconfiguration, or data clear in this verification.
- Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w06-accepted/` (`unit.log`, `build.log`, `audit.log`, `integration.log`). Worker/I01 slots released. W06 worktree retained; no further worker work needed.
- W01–W06 and W09 are accepted. Three screens remain: W07 corrections, resumed W08 finale/charts, paused W10 projector. Full integrated visual matrix, persisted screenshots, actual 200% zoom, real reduced-motion preference, two/21-team classroom/reconnect/privacy checks, Node20 parity and historical runtime limitations remain pending. No final acceptance or deployment claimed.

## W07 correction checkpoint and slot — historical, resolved above

- Clean source checkpoint `a024d3732e1884884f455b2e77a7ea35bbc81ab6`; final completion note pending. Worker reports external3/3, owned5, full122 unit/types/audit/build PASS. Coordinator unchanged external fixture independently PASS3/3 (1.60s); evidence `evidence/i01-w07-review/correction-regression.log`. Emulator rerun and final note-inclusive submission remain pending.
- Exclusive correction emulator grant recorded above. At most two implementation workers remain active: W07/W08; W10 stays paused.
- Correction tests were added under `app/src/pages/`, outside the packet’s permitted new-file directory. Move both scope test files into the owned `app/src/components/results/` directory, adjust relative imports, and rerun affected checks before final submission. No product scope or shared API expansion is authorized.

## Initial W07 review — historical, corrected and merged above

- Clean submitted HEAD `bce8b31f76e552d339b89925b430234f9bb1032f`, implementation `658829c6c9e214f24e4c5f69c2aeec4e30f765f4`. Original frozen ancestry, all twelve owned files, optional-only StandingsTable props, and whitespace PASS. Worker reports 117 unit tests, types/audit/build and 3-file/5-test integration PASS; browser fixture `AjzchN7blbth5BparAaq` / `AJZCHN` with six teams. Inspected 1366×900 and 1024×768 are not the exact final 1366×768 and 1024×720 targets; final saved visual matrix remains pending.
- Coordinator external page regression fixture **3/3 FAIL** at intended assertions. Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w07-review/results-review.test.tsx`, `vitest.review.config.mjs`, and `regression.log`. Run from W07 app: `npx vitest run --config /private/tmp/salary-showdown-ui-worktrees/evidence/i01-w07-review/vitest.review.config.mjs`. No worker files, browser, or emulator state were changed.
- **Ambiguous bargain stats:** two teams both named Tigers play each other and both roster non-exclusive pid17. Their CSV lines contain 20 and 40 points. The award attributed to team-a renders pooled **30.0 pts** because game_id + name + pid still selects both sides. Do not attribute ambiguous raw rows to an assumed team ID. Preserve the authoritative award winner and known contract facts; omit/qualify ambiguous computed line or use an actually unambiguous existing mapping without changing shared/server contracts. Add owned duplicate-name/same-pid matchup coverage.
- **Private auction feedback crosses viewer scope:** switching from team-a to team-b while the latter snapshot is pending still renders team-a's cap skip as 'Your winning bid'. The existing listener pattern carried through the refactor retains the old document; this is an uncovered privacy acceptance gap, not a claim that the old listener itself was newly introduced. Key/guard rendered snapshot state by game/round/team/actual UID, suppress stale state immediately during render, and ignore callbacks from retired subscriptions. Keep listener errors observable. Cover team/UID/game changes and delayed callbacks.
- **Historical local cache omits user scope:** StandingsPage stores loaded data under gameId/round only. A same-game UID change with getRound pending keeps the old table visible although the shared facade invalidates its user cache. Scope the local loaded key by actual UID, render a loading/unavailable state until the matching result arrives, and reject retired fetch results; preserve permitted historical selection and live SIMULATE prefix behavior.
- W07 remains unmerged and corrects locally while W06 holds the exclusive mutable slot. Add owned page regressions, rerun unchanged external fixture and independent checks, request a correction slot when ready, then submit revised note-inclusive HEAD and explicit release. W08/W10 remain paused. Full visual/classroom/zoom/reduced-motion acceptance stays pending.

## W07 reported readiness failure — coordinator endpoints reachable — 2026-09-17

- At slot start W07 reported connection refused / HTTP000 on all four endpoints; no mutable integration suite ran and no restart/reconfiguration/data clear occurred. W07 retains the grant.
- I01 read-only listener check found original Firebase PID98308 on hub4400/Auth9199/Functions5101 and Firestore PID98420 on8180. Read-only requests with approved `require_escalated` localhost access returned **4400/9199/8180 HTTP200, 5101 root HTTP404**. Functions root404 establishes service reachability, not callable success. This does not establish an outage; sandbox networking is the likely cause, matching the earlier W05 incident.
- I01 instructed W07 to retry readiness and its authorized fresh-DEV integration command using approved localhost access, preserve the failed initial access result, and report exact failures if they persist. No coordinator mutations or restart were needed. W06 remains queued next; W08/W10 remain paused.

## W06 preliminary checkpoint review — historical, accepted and integrated above

- Reviewed clean source HEAD `d2f03cc52dde5221f06f06e4fa6b65453fe3a3f4` on `codex/ss-ui-w06`; original frozen-base ancestry, all nine changed files within ownership, and whitespace PASS. Completion note is not yet submitted; worker explicitly reports a source checkpoint.
- No source blocker identified. Simulation consumes the shared presentation prefix/rows/revealAll without a second clock or server mutation; unrevealed scores are absent from rendered content. Completed own matchups filter only from the permitted prefix, and expansion selects the full game_id feed with all 23 raw columns. Duplicate names retain both teams' rows and show neutral guidance; missing round data retains franchise/round context. Actual visual and accessibility acceptance remains deferred.
- Coordinator focused `SimulatePage.test.tsx` plus `matchRows.test.ts`: **2 files / 4 tests PASS**, 2.22s. Worker reports full serial **22 files / 114 tests PASS**, types/audit/build PASS, targeted three-file test command 9/9. No coordinator emulator/browser mutations or worker file edits occurred.
- W06 awaits W07's explicit release and a direct I01 grant before its requested simulate integration suite. W07 holds the current slot. W08/W10 remain paused; no additional worker resumes until capacity is available. Final W06 acceptance/merge awaits committed completion and passing emulator evidence.

## Corrected W03 and W04 accepted — combined integration complete — 2026-09-17

- W03 accepted final HEAD `0938a466c5fcc0863198cb682ae3a48d174ccfc6`, supplemental implementation `35570664eb61cd39622ef6fda67574b939bc22a8`; whole-branch I01 merge `b0c5762e92bc8a43c147b18616050dc143d98e8b` includes W04 and all previous accepted work. Clean worker, frozen-base ancestry, all ten files owned, unchanged shared/backend/dependencies/config, and whitespace PASS.
- All W03 findings resolved: modal initial focus is Keep player, busy/rejection/container focus stays contained, exact trigger/fallback restoration and inline errors persist, actual UID participates in scope, async actions use scope generations and a synchronous exact-token single-flight guard, and saved renewal terms come from the live contract or acknowledged submitted terms. Coordinator unchanged combined external fixture **11/11 PASS**, 1.58s. Owned regression coverage includes stale success/rejection, retry and real callable rejection after live roster removal.
- Integrated frontoffice plus auction browser-SDK emulator suites: **2 files / 4 tests PASS**, 31.62s, sequential fresh DEV games, existing non-fatal Firebase Admin MetadataLookupWarning only. This completes the queued I01 W04 integration check as well. No service restart, configuration change, or data clear; harness does not print fresh game IDs.
- Integrated TypeScript/build PASS (145 modules, existing >500kB bundle warning); UI audit **106 files PASS**. Initial serial unit suite, running concurrently with the I01 emulator suite, **FAIL: 165 passed / 1 timed out** at unchanged 5s limit (`CutPreview` remaining-obligations case took 12.44s; total 45.10s). Full unit rerun with no competing I01 suite **PASS: 31 files / 166 tests**, 38.79s, unchanged assertions/config/timeouts. This supports scheduling contention but does not reclassify the failed run. Future I01 suites should run separately; stable final full verification remains required.
- Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w03-accepted/` (`unit.log`, `unit-isolated.log`, `build.log`, `audit.log`, `integration.log`). W04 earlier local evidence remains in `evidence/i01-w04-accepted/`.
- W01–W05 and W09 are now accepted; four remaining screens are W06/W07/W08/W10. W06 and resumed W07 are the two active implementation workers, W08/W10 remain paused. I01 releases the mutable slot. Full saved visual matrix, actual 200% zoom, real reduced-motion preference, representative 21-team classroom/privacy checks, Node 20 parity and historical runtime/emulator limitations remain pending. No final acceptance or deployment is claimed.

## W03 correction checkpoint review — historical, resolved above

- Clean checkpoint `f0701cd78f60d78f5e39b837307a18452372df20` preserves original ancestry and ownership; whitespace PASS. Worker reports 130 unit tests, targeted 14 tests, types/audit/build PASS. No correction-time emulator mutations have occurred; W03 requested the next slot after W04.
- Original unchanged coordinator fixture **9/9 PASS**, 1.38s. Saved terms now prefer the authoritative renewal, UID participates in action scope, scope generations suppress old async state writes, and the cut modal handles initial focus, normal wrapping, restoration, and inline rejection feedback. Original three review groups are resolved in their reproduced cases.
- Supplemental local tests **9 previous PASS / 2 new FAIL**, 1.38s. Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w03-review/frontoffice-supplemental-review.test.tsx`, `vitest.supplemental.config.mjs`, and `supplemental.log`. Run from W03 app: `npx vitest run --config /private/tmp/salary-showdown-ui-worktrees/evidence/i01-w03-review/vitest.supplemental.config.mjs`.
- **Duplicate same-frame actions:** two re-sign button clicks inside one React render frame invoke signPlayer twice. Add a synchronous scope-aware single-flight guard in the common action runner, preserving normal retry after rejection and preventing old completions from releasing a newer operation's guard. Cover actual deferred page actions, including cut as appropriate.
- **Focus escapes after a rejected cut:** busy state focuses the dialog container. When the call rejects, controls re-enable but focus remains on that container; Shift+Tab escapes because the trap handles first/last controls and outside focus but not the dialog itself. Handle the dialog container in both directions and/or restore focus to an enabled in-dialog control when busy ends; cover rejection/retry keyboard navigation without losing visible error feedback.
- W03 remains unmerged. Correct locally while W04 retains the sole mutable slot, add owned regressions, rerun both coordinator fixtures and independent checks, then perform the requested frontoffice rerun after a separate grant. Four other workers remain paused.

## Initial W03 review — historical, corrected and merged above

- Submitted clean HEAD `301c289fa44ca11278f0db9fe77e333efb7f0370`, implementation `96f56bb3ff2e92b3ffd03ac6256e13a2425e4e86`. Ten changed files are owned; original frozen-base ancestry and whitespace PASS. Worker reports 123 unit tests, two frontoffice integration tests, types/audit/build PASS. Initial live-cut integration failure and later passing regression remain recorded in the worker note; browser coverage was join-screen loading only, not a frontoffice visual pass.
- Coordinator copied the four existing owned page tests and added five regression cases outside the worker: **4 existing PASS / 5 new FAIL** at their intended assertions. Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w03-review/frontoffice-review.test.tsx`, adjacent `vitest.review.config.mjs` and `regression.log`. Run from W03 app: `npx vitest run --config /private/tmp/salary-showdown-ui-worktrees/evidence/i01-w03-review/vitest.review.config.mjs`. These local tests use mocks and no emulator/browser mutations.
- **Cut dialog keyboard behavior:** opening leaves focus on the obscured Review cut trigger; Tab from Keep player escapes the modal. Add initial safe focus, bidirectional containment including busy states, Escape/cancel handling when allowed, and restoration to the exact trigger or sensible remaining control if the cut removes it. Preserve the worker's already-applied snapshot regression and show actionable failure feedback accessibly while the modal remains open.
- **Async and identity scope:** receipt scope omits auth UID, local decisions/selection/terms/cut modal are not reset by scope changes, and post-await state writes are unguarded. A pending old-game re-sign resolves after switching to another game and marks the new game's player Re-signed, removing its action button; a same-role auth UID change inherits the previous user's success receipt. Scope the local workspace, errors/busy state and all post-await results to game/round/phase/team/actual UID. Keep shared hooks/contexts unchanged and add owned deferred-call coverage.
- **Saved renewal terms:** the success detail uses `renewalFor` with local default duration rather than the authoritative active contract. A live round-2 three-round renewal at the correct $9.2M annual rate renders the one-round $10.8M rate after reload or a teammate's action. Display the saved contract rate/duration; show pending acknowledgment accurately when the snapshot has not arrived. Add an owned live-snapshot/reload regression.
- W03 remains unmerged and may correct locally while W04 owns the exclusive mutable slot. Request a correction slot only after independent fixes/checks are ready. Commit owned fixes/tests plus the revised completion note and report final SHA directly; no rebase, shared API changes, or worker creation. Full visual/classroom checks remain pending.

## Corrected W04 source acceptance — historical, integrated emulator rerun passed above

- Accepted worker HEAD `5302aa1057721a29680350142bbaa8bf1b313e24`, supplemental source `eab4a125ac58bf2ce39d5f1f2aea30db1e19269f`; whole-branch merge `af3faf165f1e55f3b60e5c0e8a88b124c9c6362b`. Clean worker, original frozen ancestry, all ten owned files, unchanged shared/backend/dependencies/config, and whitespace PASS.
- Supplemental code preserves latest observed private snapshot after an older callable acknowledgment, retaining divergent local offers as unsaved/resubmittable; synchronous pending guard prevents duplicate calls. Owned tests additionally verify retry after rejection and auth-scope invalidation. Coordinator reran all five external review cases: **5/5 PASS**, 1.34s.
- Integrated serial unit suite **28 files / 143 tests PASS**, 17.39s; TypeScript/build PASS with existing bundle warning; UI audit **102 files PASS**. Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w04-accepted/` (`unit.log`, `build.log`, `audit.log`). Worker reports fresh auction integration **1/1 PASS**, 5.22s. I01 integrated emulator rerun is explicitly pending because W03 holds the slot; no competing mutations occurred.
- All W04 review findings are resolved. W04 worker is finished and its worktree remains intact. W03 and resumed W06 occupy the two implementation slots; W07/W08/W10 stay paused. Full visual/classroom/zoom/reduced-motion/privacy acceptance remains deferred and unverified; no final acceptance or deployment claimed.

## W04 corrected submission review — historical, resolved above

- Clean submitted HEAD `6b0c11af8dc781e43cdd0d78f297b5ced2a1ce5e`, correction source `766409434bc0c9b02f6029232bb023e4ccd28f9c`. Original ancestry, owned-file changes, and whitespace PASS. Worker reports serial 120 unit tests, types/audit/build and auction integration PASS (1 test, 4.53s); live DEV join code `NVWLYW` used for five-card/keyboard/SEALED/revision/withdrawal checks. Full visual matrix remains deferred.
- Original unchanged coordinator fixture **3/3 PASS**, 1.12s. The correction adds auth UID scope and raw per-player edit versions; original three findings are resolved. However, source review found two additional save-path failures; supplemental fixture **3 existing PASS / 2 new FAIL**, 1.03s. Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w04-review/auction-supplemental-review.test.tsx`, `vitest.supplemental.config.mjs`, `supplemental.log`. Run from W04 app: `npx vitest run --config /private/tmp/salary-showdown-ui-worktrees/evidence/i01-w04-review/vitest.supplemental.config.mjs`.
- **Newer teammate snapshot overwritten by older acknowledgment:** saved 3, submit 8, receive own snapshot 8 then newer teammate snapshot 9 before resolving the original callable. Success unconditionally replaces savedRef with payload 8 and clears dirty, leaving the visible 8 marked Saved/SEALED and impossible to submit while current server value is 9. Preserve latest observed server state; reconcile a completed unedited submission to the newer snapshot or keep the divergent local offer explicitly unsaved and resubmittable. Preserve any newer local edit.
- **Duplicate same-frame submissions:** two button clicks in one render frame produce two submitBids calls. `busy` state is not a synchronous guard and pendingSubmissionRef is assigned but not checked at entry. Add a synchronous scope-aware single-flight guard; retain correct retry after rejection and invalidation on scope changes. Test actual page behavior with deferred calls.
- W04 remains unmerged. Supplemental fixes and owned regressions are required; rerun local checks, both coordinator fixtures and reserved auction integration. Worker released its initial slot; I01 explicitly regrants a correction slot above. No coordinator emulator/browser mutations occurred during review.

## W04 source checkpoint review — historical, corrected and integrated above

- Checkpoint `3c1f4fcee06af48a1a1c22efb0fa3345b5a53465` is clean, based on the exact frozen base, and changes eight owned files; no completion note yet (worker explicitly submitted a source checkpoint). Ancestry and whitespace checks PASS. Worker reports serial unit **22 files / 118 tests PASS**, TypeScript PASS, UI audit **85 files PASS**, build PASS with existing bundle warning. No emulator suite has run; W03 owns the mutable slot and W04 remains queued.
- Coordinator external page regressions **3/3 FAIL** for the intended assertions (1.19s). Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w04-review/auction-review.test.tsx`, `vitest.review.config.mjs`, and `regression.log`. Run from W04 app: `npx vitest run --config /private/tmp/salary-showdown-ui-worktrees/evidence/i01-w04-review/vitest.review.config.mjs`. Only local mocks/unit rendering were used; no worker files or emulator state were changed.
- **Newer typing is lost during a pending save.** Saved rate 3, submit rate 8 with deferred success, type 3 again, receive the saved-8 snapshot, resolve the call. The editor incorrectly returns to 8. `updateOffer` removes the dirty flag by comparing against the old saved baseline, allowing the pending snapshot to erase the later edit. Preserve edits made after submission regardless of matching the previous baseline; clear dirty only for the exact acknowledged generation/values. Keep the later edit unsaved and suppress a clean SEALED treatment.
- **Invalid raw input is treated as clean after rounding.** Saved rate 2.4, type invalid 2.35, receive an unrelated same-value snapshot. The input becomes 2.4 and its validation error disappears because `toBids` rounds before dirty comparison. Dirty tracking and acknowledgment must preserve exact raw edits/validation state, including invalid or transient input; payload normalization must not determine whether editing is clean.
- **Receipt scope omits actual user identity.** It uses membership role instead of auth UID. Change user-a to user-b with the same game/round/phase/team/role while a save is pending, then resolve it: user-b sees the old SEALED receipt. Consume the existing useAuth UID and scope editor state, listeners, pending results, and receipt appropriately without changing shared contexts/hooks.
- W04 should add owned page-level deferred-call regressions for these cases plus the packet's explicit server-rejection-without-SEALED case, then rerun independent checks while awaiting its separate emulator grant. Pure helper tests do not exercise the editor lifecycle. Do not merge this checkpoint or widen ownership/dependencies/contracts.

## Corrected W09 accepted and integrated — 2026-09-17

- Accepted worker HEAD `6e49abead91f799ffa623d24108f57f44ed9779d`; correction implementation `9768251dab54efae848afaf80676c0d5edc85b25`; whole-branch I01 merge `3127f5425a903ce54c940f27450989fbefaceed9`. Worker clean status, original frozen-base ancestry, all 14 files within ownership, and whitespace checks passed. No shared/backend/dependency/config changes.
- Both SeatPanel review defects are resolved. Opening initialization is scoped to open/session/explicit target changes, valid selection survives unrelated snapshots, armed confirmation tracks the observed holder UID and invalidates when that holder changes, and Tab wraps in both directions. The owned integration regression preserves live selection/focus, tests holder replacement and two-click release, and checks exact-trigger focus restoration.
- Coordinator reran the unchanged external fixture on submitted HEAD: **2/2 PASS**, 1.37s. Integrated serial unit suite: **25 files / 131 tests PASS**, 23.26s. Integrated TypeScript/build PASS (existing >500kB bundle warning); UI audit **98 files PASS**. Integrated professor, professor-seats, professor-fixes, and finale suites: **4 files / 16 tests PASS**, 44.98s, using the browser-SDK alias and fresh DEV games sequentially. Existing non-fatal Firebase Admin MetadataLookupWarning persists. Harness did not print fresh game IDs.
- Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w09-accepted/` (`unit.log`, `build.log`, `audit.log`, `integration.log`). External reproduction source/config remain in `evidence/i01-w09-review/`. W09 reports real-browser bidirectional Tab wrapping and Escape/trigger return on fresh game `yCQYNiTlTsadp38jfcLH` / `YCQYNI`; its tab/server were closed. No restart, reconfiguration, or data clear occurred.
- W01, W02, W05, and W09 are integrated; six screens remain: W03, W04, W06, W07, W08, W10. All six have registered directly. User requested reduced concurrency: W03/W04 continue, W06/W07/W08/W10 pause at safe checkpoints. Full integrated visual/classroom checks, persisted screenshots, actual 200% zoom, real reduced-motion toggle, 21-team representative drill, Node 20 parity, and historical broader runtime/emulator limitations remain pending. No final acceptance or deployment is claimed.

## Initial W09 review — historical, corrected and merged above


- Reviewed submitted note-inclusive HEAD `160cbec8067c12edca219f38283a19807294fe53` (implementation `741a1f3bb3cd3162fda1f0465d40d72ae1eaf1d7`). Clean worktree, frozen-base ancestry, ownership of all 14 changed files, and whitespace checks pass. I01 remains at product merge `638775233d419cd6cb44223c521dd25305b353ab`.
- Coordinator verification on W09: serial unit suite **20 files / 110 tests PASS** (9.90s), TypeScript PASS, UI audit **83 files PASS**. External SeatPanel regressions: **2 tests / 2 FAIL** (1.25s). Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w09-review/seat-panel-review.test.tsx`, adjacent `vitest.review.config.mjs` and `regression.log`. Run from W09 app: `npx vitest run --config /private/tmp/salary-showdown-ui-worktrees/evidence/i01-w09-review/vitest.review.config.mjs`.
- **Correction 1 — unrelated live team updates reset seat management.** SeatPanel's opening effect depends on `teams`; a new snapshot reselects the initial/first team, clears the armed release, and focuses Close. Reproduction: select Beta, arm Release Scout on Beta, then update only Alpha's Done state; selection resets to Alpha. Initialize on opening or explicit target/session change and reconcile removed teams without resetting valid selection, confirmation, or focus on unrelated snapshots. Add an owned deferred/live-update regression.
- **Correction 2 — modal keyboard focus escapes to background controls.** SeatPanel declares `aria-modal` but does not contain keyboard focus. Reproduction: focus Close seat tools and press Shift+Tab; focus reaches the background control outside the dialog. Enforce modal focus containment in both directions and preserve Escape, Close, and return to the invoking control. Add owned keyboard regressions and browser verification.
- Worker reports professor integration 3 files / 13 tests, finale 3 tests, build, HTTP Done/revision, and 21-team browser checks passing. These do not cover the two reproduced regressions. I01 did not mutate emulator/browser state during this review. W09's supplemental backend test was not run because backend dependencies were unavailable. Full visual/classroom acceptance, saved screenshots, actual 200% zoom, and real reduced-motion verification remain pending.
- W09 remains unmerged until corrected source and regressions pass. Continue in the existing W09 task without rebasing, reset, shared API/dependency/config changes, or new agents. Commit owned corrections/tests and revised completion note; rerun unit/type/audit and the reserved professor integration checks, then report final note-inclusive HEAD and explicit slot release.

## Corrected W01 accepted and integrated — 2026-09-17

- Accepted immutable worker HEAD: `00dc7bcf39ab3135406bdb95f5d8d91ebe6c3d96`; correction implementation `0a11efca3ae043ae4f46f1a2187206c08a4817c8`. Whole-branch merge: `638775233d419cd6cb44223c521dd25305b353ab`, including accepted W02/W05 and foundations. Worker remains clean and untouched; remaining workers retain the original frozen base.
- Reviewed the correction diff. IdentityEditor tracks incoming identity revisions and reconciles a newer observed snapshot after a successful pending save, while preserving the submitted preview against the unchanged original prop and retaining rejected drafts. Numeric sorting coverage now includes separate own-team and rival Franchise 2/Franchise 10 before and after rename. Both review findings are resolved. Ownership, frozen-base ancestry, unchanged shared/backend/manifests/locks, and whitespace checks passed.
- Coordinator unchanged external fixture: PASS, 5/5 including the previously failing identity race. Submitted-head unit: PASS, 21 files / 115 tests (8.18s); TypeScript PASS; UI audit PASS, 87 files.
- Integrated landing+lobby browser-SDK suite: PASS, 2 files / 5 tests (35.75s), with only the existing non-fatal Firebase Admin MetadataLookupWarning. Existing harness created fresh DEV games but did not print IDs. Integrated TypeScript PASS; audit PASS, 96 files; build PASS with the existing >500kB bundle warning.
- Initial integrated parallel unit run: **FAIL**, 23 files passed / 2 failed, 129 tests passed / 2 timed out at the unchanged 5-second limit. Failures were the existing market CompareTray removal and ContractComposer future-cap tests; market source is byte-identical to the earlier accepted candidate. Full serial rerun `npm test -- --no-file-parallelism`: **PASS, 25 files / 131 tests (38.98s)**. This supports resource contention as the explanation but does not reclassify the failed parallel run. No assertions, timeouts, retry policy, or configuration changed. Stable final all-screen verification remains required.
- Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w01-accepted/` (`integration.log`, `unit.log`, `unit-serial.log`, `types.log`, `audit.log`, `build.log`). No service restart, backend changes, or data reset. W01 and I01 verification slots are released; W09 receives the next slot above.
- The worker's reported same-origin multi-tab hydration observation remains unresolved for final classroom testing; no cause is asserted and it is not counted as passed. Full integrated visual/21-team review, screenshot artifacts, exact viewport review, actual 200% zoom, real reduced-motion toggle, season/classroom checks, and previous runtime/emulator limitations remain pending. No final acceptance or deployment is claimed.
- W01, W02, W05 are now integrated. Remaining seven screen tasks: W03, W04, W06, W07, W08, W09, W10. Recommended next task: W09 professor control desk; W03/W04 may start independent work concurrently.

## Initial W01 review — historical, corrected and merged above

- Submitted immutable HEAD: `f3e039d14b23a6621019c89993d336220173c80f`; implementation `df38e701b72dec8713bcf0910a7beccc39ce3672`. Clean worktree, exact frozen-base ancestry, permitted ownership (eleven implementation/test files plus note, including task-local CSS), unchanged shared/backend/manifests/locks, and diff whitespace checks passed. Worker source remains untouched by I01.
- Coordinator checks: full unit PASS, 21 files / 114 tests (5.25s); TypeScript PASS; UI audit PASS, 87 files. Worker targeted emulator checks are documented in its completion; I01 has not run emulator checks on this submission.
- **Correction 1 — newer teammate identity is lost during save acknowledgment.** `IdentityEditor.tsx:32–36,45–52` ignores incoming snapshots while dirty, then clears dirty after success without reconciling a newer already-received identity. Reproduction: start gold/classic, choose/save teal/classic; receive the own teal snapshot, then teammate coral/stripe snapshot before the callable resolves; resolve success. The editor remains teal/classic, Save is disabled, and it says saved despite the current server identity being coral/stripe. Reconcile newer received snapshots after completion while retaining the submitted preview when only the original stale prop is available, and retain rejected drafts. Add an owned deferred-call regression. External fixture copied four existing IdentityEditor tests plus the new case: **4 pass / 1 fail**. Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w01-review/identity-review.test.tsx`, adjacent config and `regression.log`. Run from W01 app: `npx vitest run --config /private/tmp/salary-showdown-ui-worktrees/evidence/i01-w01-review/vitest.review.config.mjs`.
- **Correction 2 — restore numeric-aware rival-sort regression coverage.** `itest/lobby.itest.tsx:47–67` now makes Franchise 10 the own hero and asserts only Alpha/Beta/Franchise 2 in the rival grid. This passes even with lexical sorting and therefore no longer preserves the former 2-before-10 assertion, despite its comment. Add both Franchise 2 and Franchise 10 as rivals (using a separate own-team name) and assert numeric order before and after rename, retaining server-write/rival-isolation checks.
- Worker reported two same-origin browser tabs that completed server joins but did not hydrate their lobbies; a separate localhost-origin client rendered. This is an unresolved browser/classroom observation, not a diagnosed provider defect and not a pass. Preserve it for integrated classroom verification alongside the user-deferred visual checks. No claim that unit/integration passes resolve it.
- Historical disposition: W01 was held out of I01 until corrections passed review (now resolved in the acceptance record above). No API/ownership exception needed. Continue in the existing task, commit owned fixes/tests and revised note, rerun unit/type/audit and reserved landing+lobby integration, report final note-inclusive HEAD and slot release. Full visual/classroom acceptance remains pending.

## Corrected W05 accepted and integrated — 2026-09-17

- Accepted immutable HEAD: `f4d484814ede3cea17205f089e5ab76c18331f93`; correction implementation `71521d3f1ae309449466981fd0cc46dd8a1d54d5`. Whole-branch merge: `f517eff2ff721ec9ca2da18cdd903b915e96de4f`, including W02 and all accepted foundations. Worker remains clean and untouched; remaining screen worktrees retain the original frozen base.
- Reviewed the correction diff and added regressions. The local lineup workspace is keyed to game/round/phase/team/actual uid, and that same scope reaches the shared receipt hook. Old success, error, and pending cleanup affect only the unmounted old workspace; same-scope drafts and locked-lineup initialization remain intact. Both prior blockers are resolved without shared API changes. Ownership, ancestry, manifests/locks/backend invariance, and diff whitespace checks passed.
- Coordinator reran the unchanged independent review fixture: PASS, 8/8 tests (including the prior two failures). Submitted-head full unit: PASS, 21 files / 119 tests; TypeScript PASS; UI audit PASS, 86 files.
- Integrated candidate: full unit PASS, 24 files / 126 tests (4.46s); TypeScript PASS; UI audit PASS, 90 files; build PASS with the existing >500kB bundle warning. Sequential browser-SDK lineup + market integration PASS, 2 files / 8 tests (17.45s), with only the existing non-fatal Firebase Admin MetadataLookupWarning. Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w05-accepted/` (`unit.log`, `types.log`, `audit.log`, `build.log`, `integration.log`). No assertion retries/configuration changes, emulator restart, data reset, or backend edits. Market fixture IDs printed by the suite: `9orRVOODLW5gHl29yJIl`, `5zz77n4tJ05xhqfsimRR`, `hNG9fRE5LyP6XjV2jEXY`, `T2jb0kIKKpsyXedxuKSw`; the lineup harness generated fresh games but did not print their IDs.
- Worker browser evidence and deferred checks remain in `completions/W05.md`. I01 did not repeat browser visuals during correction review. Saved screenshots, actual 200% zoom, actual reduced-motion toggles, physical trackpad inspection, dedicated long-name inspection, full integrated visual/classroom/season acceptance, and previously recorded emulator/runtime limitations remain pending. No final acceptance or deployment is claimed.
- W05 and I01 verification slots are released. W01 receives the next exclusive slot as recorded above. W02 and W05 are complete at the source/integration gate; the other eight screens remain outstanding.

## Initial W05 review — historical, corrected and merged above

- Submitted immutable HEAD: `21c8bca2fb67f44268fe06579f76106301d6398f`; implementation `d5bf96c`. Exact frozen-base ancestry, clean worktree, exclusive ownership (eight implementation/test files plus note, including permitted task-local CSS), unchanged foundations/backend/manifests/locks, and whitespace checks passed. No worker source was modified by I01.
- Coordinator submitted-head checks: `npm test` PASS, 21 files / 116 tests (3.83s); `npx tsc -b --pretty false` PASS; `npm run audit:ui` PASS, 86 files. Worker integration/browser evidence remains recorded in its completion note; I01 has not run emulator tests for this submission.
- Review reproduced two failures of the required scoped save acknowledgment. External fixture copies all six existing CourtSlot/page tests with absolute imports and adds two deferred-call cases; result **6 pass / 2 fail**. Fixture: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w05-review/lineup-review.test.tsx`; configuration and `regression.log` alongside it. Run from W05 app: `npx vitest run --config /private/tmp/salary-showdown-ui-worktrees/evidence/i01-w05-review/vitest.review.config.mjs`.
- **Correction 1 — actual authenticated identity is missing.** `LineupPage.tsx:150–153` scopes the receipt to game/round/phase/team/role/displayName, but the frozen contract requires authenticated user identity. With a save pending, change uid while membership labels remain the same: resolving the old call renders both the receipt and success status for the new user. Use actual `useAuth().uid`; displayName/role are not a user identifier. Add a deferred-call uid-change regression with unchanged labels.
- **Correction 2 — success status bypasses the scoped receipt guard.** The unconditional `setPlacementMessage` after `await run(...)` publishes success even when the hook suppresses a stale receipt. With a save pending, change gameId and resolve the old call: the new game displays “Lineup saved for round 1.” Scope/guard all save-related async UI state, including rejection and pending cleanup, so old requests cannot alter the new scope. Add deferred success/rejection scope-change regressions. A page-local workspace keyed by game/round/phase/team/uid (as W02 does) is a valid approach; preserve same-scope drafts and locked remount behavior, without editing shared hooks.
- Historical disposition: W05 was held out of I01 until correction review passed (now resolved in the acceptance record above). No frozen contract change is requested. Continue inline in its existing worktree, add regressions in owned tests, rerun packet unit/type/audit and reserved integration checks, update its completion note, and report the revised note-inclusive HEAD and slot release. Full visual/classroom acceptance remains deferred and pending.

## W02 accepted and integrated — 2026-09-17

- Accepted immutable worker HEAD: `fe023799cf939fa46392d765ed5bd22cf36bc51f`; implementation `ef8f6b146fb461e9bd22a0b3eb6fce5e8eae5e2c`. Whole-branch merge into I01: `1556538f738f5f442547f8472eb70679cf13facf`. Worker worktree is untouched and clean. All remaining screen worktrees retain the frozen starting SHA.
- Reviewed all implementation/test changes and committed handoff. No blocking source findings or contract deviations. Exact-base ancestry, permitted paths (nine implementation/test files plus W02 note), unchanged shared foundations/backend/manifests/locks, and diff whitespace checks passed. Two added tests are within the owned market component directory.
- Market now has persistent catalog/detail/contract workspace, up to three raw-stat comparison columns, keyboard sorting/selection/removal focus, five-round payroll preview, guarded signing and scoped receipts, and responsive factual roster display. Ordinary FA remains nonexclusive; Done remains revisable; role fallback, synthetic exclusions, unsold asks, and future-cap restrictions remain covered. No optimistic roster/payroll mutations.
- Coordinator submitted-head checks: `npm test` PASS, 23 files / 117 tests (3.50s); `npx tsc -b --pretty false` PASS; `npm run audit:ui` PASS, 85 files. Merged Salary Showdown source is byte-identical to the tested worker HEAD; there were no merge conflicts.
- Coordinator merged-head check: `npx vitest run --config vitest.integration.config.ts src/itest/market.itest.tsx` PASS, 1 file / 4 tests (8.89s). Evidence: `/private/tmp/salary-showdown-ui-worktrees/evidence/i01-w02/integrated-market.log`. Fresh game IDs: `NTs5zMWCpYF9cHhFaVtl`, `FVFfbgxgrxyabhdDM26L`, `OIdwwNcvgpK5rSAcAPPO`, `wBWj0OaaGVa75t3exPYU`. Only the non-fatal Firebase Admin MetadataLookupWarning appeared. No test retries/configuration changes, service restarts, data resets, or backend changes.
- Worker build and browser interactions/viewport measurements are documented in `completions/W02.md` and `/private/tmp/salary-showdown-ui-worktrees/evidence/w02/`. These remain worker evidence; I01 did not repeat browser checks during this review. Full integrated visual review, saved screenshots, actual 200% zoom, actual reduced-motion toggles, all-screen season/classroom acceptance, and existing emulator/runtime limitations remain pending. No final acceptance or deployment is claimed.
- W02 and I01 verification slots are released. W05 receives the next exclusive slot as stated in the current reservation above. W01/W03/W04/W06–W10 continue independent work and wait for a later explicit mutable-test grant.

## Corrected F03 acceptance — completed source gate

- Submitted and accepted F03 head: `8d96847b6cbdbcdf3636a7ca19eb5f1e4744de08`; correction implementation: `f2e4451e86065cb8e1570d4af0dfa4876ce1ec7b`.
- Whole-branch merge: `ecdf667ae9ea940284c34aa15d52cd0b40ef6a79`. Integration-only lobby selector fix / current candidate: `b48cdf620c594aba0386fa6775af86aea40da401`. This contains F01, F02, and corrected F03. It is now the frozen shared base, as recorded in the current release above. Later I01 commits update this ledger only.
- Coordinator reviewed the complete correction diff and new regressions. Both previously reproduced defects are fixed without changing frozen interfaces or ownership. Exact-base ancestry, permitted file ownership, unchanged manifests/locks, and diff whitespace checks passed. Worker branch remains untouched and clean.
- Independent coordinator reproducer: PASS, 1 file / 9 tests, including both prior failures. Submitted-head frontend: PASS, 20 files / 110 tests; TypeScript PASS; UI audit PASS, 81 files.
- Integrated candidate frontend: PASS, 20 files / 110 tests; TypeScript PASS; UI audit PASS, 81 files; build PASS with the existing large-bundle warning. Backend identity: PASS, 1 file / 23 tests. Backend source is byte-identical to the prior HTTP-verified foundation backend; no emulator restart or backend source swap occurred.
- Initial integrated full browser-SDK suite: 19 files / 39 tests passed; one lobby rename test failed because the new header and lobby card both contain the renamed team text. The behavioral rename and rendered team card succeeded, but the global `getByText` query was ambiguous. Full suite rerun after the integration-only selector fix: 19 files / 39 tests passed; one contexts test failed with Firebase `deadline-exceeded` (295.74s total). The emulator log records a `joinGame` HTTP function timing out after about 60s during seed setup before the UI render; no changed backend code is involved. Subsequent unchanged targeted contexts + lobby rerun: PASS, 2 files / 5 tests in 10.29s. This supports a transient emulator failure but does not turn the failed full run into a pass. Evidence: `integrated-browser-sdk-fixed.log`, `contexts-lobby-recheck.log`, and the service log near lines 10747–10751. A fully clean all-screen suite remains required at final acceptance; no assertion was removed and no retries/timeouts were added to the test configuration. Evidence directory: `/private/tmp/salary-showdown-ui-worktrees/evidence/f03-corrected/` (`review-regression.log`, `unit.log`, `types.log`, `audit.log`, `integrated-unit.log`, `integrated-types.log`, `integrated-audit.log`, `integrated-build.log`, `identity.log`, `integrated-browser-sdk.log`).
- **Historical post-review reservation: none.** F03 and I01 released their verification slots; the current W02 grant above supersedes this state. No service restart or data clear is authorized for workers.
- F03 reports exact CSS viewport measurements at 1280×720, 1366×768, 1440×900, 1024×720, and 640×360, superseding the initial larger-viewport limitation. Full persisted screenshots, actual browser zoom, and a real browser/OS reduced-motion toggle remain unverified; 640×360 is a reflow equivalent and automated provider toggles are distinct evidence. These are not silently promoted to passes.
- **Narrow integration-only ownership exception, reconciled before screen branches exist:** I01 changed only the `Cap Crunchers` assertion in `app/src/itest/lobby.itest.tsx` (normally W01-owned) to select `.card > strong`, matching the test's existing card-order assertions. No assertion was removed or weakened; the renamed lobby card, server document, rivals, and sorting remain checked. This integrates the intentional shared header without changing product behavior. W01 will inherit the fix in its starting base; no active screen branch needs rebasing. Ownership returns fully to W01 after this gate.
- Remaining source blockers: None from this correction review. The user deferred the full visual review; the current screen-release section records the exact frozen base, prepared worktrees, and unresolved final acceptance checks. Final visual/classroom acceptance remains pending.

## Initial F03 review — historical, fixed by the correction above

- Submitted immutable F03 head: `96917e0aa5558e189868d825a900d0b1f28f4de1`; implementation `6d50f6161917e25564e8ced2924f3c5c40fa104f`; accepted starting base remains `11c6c019221ea7d324571ba52e5d49be96caffe3`.
- F03 and I01 were clean at review start. Ancestry and ownership checks passed: 14 files, including the permitted tightly local `components/shell/PresentationStandingsRoute.tsx`. Manifests, lockfiles, backend, and global styles unchanged. `git diff --check` passed.
- Coordinator reran the submitted frontend suite: PASS, 20 files / 104 tests; TypeScript PASS, exit 0; UI audit PASS, 81 files scanned. Logs: `/private/tmp/salary-showdown-ui-worktrees/evidence/f03-review/unit.log`, `types.log`, and `audit.log`.
- Coordinator review fixture reproduced **two failures**, with all seven copied existing provider tests still passing. Fixture and log: `/private/tmp/salary-showdown-ui-worktrees/evidence/f03-review/presentation-review.test.tsx` and `/private/tmp/salary-showdown-ui-worktrees/evidence/f03-review/regression.log`. The fixture imports the actual submitted source; it lives outside all worktrees. No worker file was changed.
- **F03 is not merged. No base is frozen and no W01–W10 worktree/session is released.** I01's product candidate remains `11c6c019221ea7d324571ba52e5d49be96caffe3`; later I01 commits change this ledger only.

### Required F03 corrections

1. **Nondecreasing presentation breaks when reduced motion is turned off.** In `RoundPresentationContext.tsx:218–238`, reduced motion makes `applied` equal the full game count, but the higher watermark is written only to sessionStorage, not retained in live progress state. With a fresh two-game round, toggle false → true → false before the normal first reveal: `applied` becomes 0 → 2 → 0. Already revealed scores and records disappear. Preserve the scope-local in-memory high watermark across preference changes and clock changes, including when sessionStorage is unavailable. Add a fresh-state toggle regression that does not inherit a prior Reveal all flag.
2. **Historical facade publishes old game/user data during a scope change.** In `useRoundDoc.ts:7–19`, cached hook state is keyed only by round number. After `useRoundDoc(1)` resolves in game A, switching gameId/uid to game B at round 2 with its round-1 read pending still returns game A's round document. Scope the returned historical result to the current game/user/query identity and clear it synchronously on a scope change; ignore obsolete async completions. Add a deferred-read regression in an owned test file. Preserve the frozen public signatures; do not widen contracts to work around the issue.

Worker-reported targeted emulator/browser checks remain evidence for the submitted implementation, not evidence that these uncovered cases pass. Coordinator did not run mutable emulator checks in this review. Exact 1366×768/1440×900 screenshots, persisted screenshot artifacts, and a real browser reduced-motion preference toggle remain pending as disclosed by F03; they still need resolution before full foundation/visual acceptance.

### Historical correction ownership and emulator slot (released)

F03 released its prior slot in its completion note. I01 now explicitly grants **F03's correction session** the exclusive mutable emulator slot for the packet's integration/browser checks, through its revised completion or explicit release. Continue in the existing F03 worktree from submitted head `96917e0aa5558e189868d825a900d0b1f28f4de1`; do not reset to the original base or create another worktree. Record the revised immutable head, actual test state, fresh game IDs, commands/results, and slot release. Recheck service readiness and report an outage to I01; never restart the service, change emulator configuration, or clear data. Run mutable checks sequentially. Other workers and I01 must not run competing mutable suites while this grant is active. Backend source is unchanged and remains the previously verified I01 foundation backend.

Copyable correction prompt:

```text
Continue F03 inline in /private/tmp/salary-showdown-ui-worktrees/F03 on codex/ss-ui-f03. Verify clean status and current HEAD 96917e0aa5558e189868d825a900d0b1f28f4de1; do not reset or create another worktree. Read the current F03 review in the LIVE ledger /private/tmp/salary-showdown-ui-worktrees/I01/docs/superpowers/plans/salary-showdown-ui-tasks/completions/I01.md.

Fix both reproduced blockers: (1) reduced motion false→true→false must never decrease the current revealed prefix; retain a scope-local in-memory high watermark even if sessionStorage is unavailable; (2) useRoundDoc historical data must be scoped to game/user/query and return null immediately on scope changes while new data loads, ignoring stale completions. Preserve frozen public contracts and exclusive ownership. Add both regressions to owned tests before fixing. The independent reproducer is /private/tmp/salary-showdown-ui-worktrees/evidence/f03-review/presentation-review.test.tsx; its log and config are alongside it.

Rerun the packet's unit, typecheck, UI audit, targeted emulator integration, and available browser checks. I01 grants this F03 correction session the exclusive emulator slot subject to the live ledger. Use fresh DEV game IDs, run mutable checks sequentially, and do not restart emulators or clear shared data. Record remaining visual/tool limitations honestly.

Commit only owned corrections/tests plus the updated completions/F03.md. Report the final note-inclusive HEAD, actual check results, interface changes (None if unchanged), and explicit slot release. Do not merge, deploy, implement another task, or create sessions/subagents. Return the revised completion to I01.
```

## Accepted F01/F02 gate history — 2026-09-17

- Accepted F01 head: `3ae21100bb29d85dddc7e1a699673d4c4ccfc812`; whole-branch merge: `2b53480a273859571a53ebcf31762d273805b0ee`.
- Accepted F02 head: `e445e545943a3dab325d3d449b90aedba12ec99a`; whole-branch merge / tested candidate: `11c6c019221ea7d324571ba52e5d49be96caffe3`.
- Review found no blocking source or contract defects. Both heads descend from the exact bootstrap commit, modify only explicitly owned paths and their notes, and leave manifests/locks unchanged. No fragment cherry-picks or worker edits by I01.
- F03 exists at `/private/tmp/salary-showdown-ui-worktrees/F03`, branch `codex/ss-ui-f03`, exact base `11c6c019221ea7d324571ba52e5d49be96caffe3`, Vite port 5182. Frontend dependencies installed with `npm ci --offline --no-audit --no-fund`; unchanged lockfile, no production env copied. User starts this session; I01 creates no session or subagent.
- **Historical initial F03 grant (superseded by the correction grant above): F03.** I01 has completed and released its foundation verification slot; F02 previously released its slot in its completion note. F03 is explicitly granted its packet's emulator integration tests and browser scenarios, using fresh game IDs, through its completion or explicit release. Grant starts from accepted base `11c6c019221ea7d324571ba52e5d49be96caffe3`; record the actual tested worker commit/worktree state, commands, game IDs, and results in the F03 handoff. Run mutable commands/scenarios sequentially, preserve the existing browser-SDK alias, and do not restart the service or clear data. I01, F01/F02, and screen sessions may not run competing mutable checks. The stable HTTP backend already includes F02; no backend changes are needed for F03.
- No shared-base freeze or W01–W10 release yet; those require F03 and the remaining foundation checks.

### Candidate verification

All coordinator logs are under `/private/tmp/salary-showdown-ui-worktrees/evidence/foundation-gate/`.

| Check | Actual result |
| --- | --- |
| F01 submitted-head unit / typecheck / audit | PASS: 18 files / 95 tests; typecheck exit 0; 77 files scanned |
| F02 submitted-head identity, rename, create-team, advance-race | PASS: 4 files / 44 tests |
| Integrated frontend unit / typecheck / audit | PASS: 18 files / 95 tests; typecheck exit 0; 77 files scanned |
| Integrated build | PASS; existing large-bundle warning; local backend/dist only |
| Identity SDK → HTTP callable | PASS: fresh two-team lobby, save, fresh-client getLobby, legacy omission, rival isolation, stranger rejection, invalid enum rejection, post-start rejection |
| Integrated backend `npm test` | FAIL: 6 files failed; 207 tests passed, 4 failed, 3 skipped; transaction-lock/timeouts |
| Integrated backend `npm test -- --no-file-parallelism` | PASS: 28 files / 214 tests, 25.61s |
| Browser-SDK integration suite | PASS: 20 files / 38 tests, 121.46s, exit 0; existing alias/config unchanged |
| Real browser legacy-page smoke | PASS (limited scope): at 1280×720, landing lookup → open Coach seat → routed market → Standings; market screenshot inspected inline; zero captured console errors |

HTTP evidence: `identity-http.cjs` and `identity-http.log`. Unique game: `gEhS8pl1QYi3IUXC7j9K` (join code `GEHS8P`). These are actual Firebase SDK clients through local Auth/Functions HTTP, distinct from the worker's wrapped-function tests. F02's pending HTTP/client check is now satisfied.

Real-browser smoke used the same fresh HTTP verification game, with display name `I01 Browser Coach`, after all suites completed. The temporary browser tab was closed and viewport reset before releasing the slot. Broader rendered legacy behavior is covered by the unchanged 20-file integration suite. No persisted screenshot path is claimed. F01's persisted component screenshots and actual browser reduced-motion toggle remain pending foundation/final acceptance evidence; its worker-reported static reflow and automated reduced-motion checks remain distinct evidence. No visual or classroom final acceptance is claimed.

The unchanged backend suite passes when file concurrency is removed, supporting emulator contention as the explanation for the first run. The failed default run is retained in `integrated-backend.log`, with the serialized pass in `integrated-backend-serial.log`; no test assertions, configuration, retry policy, or gameplay code changed. Default parallel stability and Node 20 runtime parity remain limitations for final acceptance.

### Emulator recovery / stable backend

The original Firebase parent PID 15027 had exited before this gate; only orphaned Firestore PID 15087 remained. Auth/Functions/hub were unavailable. I01 exported Firestore successfully before stopping that owned orphan; export files are in `evidence/foundation-gate/emulator-preserved/firestore_export`. No data-clear endpoint was used. Auth state from the already-exited process was unavailable; verification uses fresh anonymous users.

I01 restored the suite against candidate `11c6c019221ea7d324571ba52e5d49be96caffe3`, importing the preserved Firestore export:

```text
firebase emulators:start --project salary-showdown-dev --only auth,functions,firestore --import /private/tmp/salary-showdown-ui-worktrees/evidence/foundation-gate/emulator-preserved/firestore_export
```

Observed Firebase PID 98308, Firestore PID 98420, coordinator command session 68575. Log: `evidence/foundation-gate/emulator.log`. Auth9199 / Firestore8180 / Functions5101 / hub4400 remain unchanged. `setTeamIdentity` is registered in us-west1. Recheck readiness on resumption; never infer readiness from historical PIDs. The service source is I01's committed backend, and no source changes occur during mutable checks.

## Immutable starting points

- Original checkout: `/Users/dylanmassaro/FenriX`, branch `main`, implementation baseline `5587b6f6f972690eaea338e32c1aba568c85613b`. Its unrelated tracked/untracked changes and original planning files remain untouched.
- Planning branch: `codex/salary-showdown-ui-plan`, worktree `/private/tmp/salary-showdown-ui-worktrees/PLAN`.
- Bootstrap commit: `2156f3a11c305600a2e58e0b4e483615d10081f2`. Its parent is the implementation baseline above. It adds only the laptop spec, master plan, and task packet directory (17 files). One trailing blank line in the copied master plan was removed for `git diff --check`.
- Coordinator branch: `codex/salary-showdown-ui-integration`, worktree `/private/tmp/salary-showdown-ui-worktrees/I01`, Vite port 5193.
- This coordination-only commit follows the bootstrap commit. Resolve its current head with `git -C /private/tmp/salary-showdown-ui-worktrees/I01 rev-parse HEAD`; it does not change either worker's base.

The master plan's lowercase branch names and named integration branch take precedence over the packets' generic `codex/ss-ui-F01`, `codex/ss-ui-F02`, and `codex/ss-ui-I01` examples. No product API contracts changed.

## Ready worktrees

| Task | Branch | Existing worktree | Starting commit | Vite |
| --- | --- | --- | --- | --- |
| F01 | `codex/ss-ui-f01` | `/private/tmp/salary-showdown-ui-worktrees/F01` | `2156f3a11c305600a2e58e0b4e483615d10081f2` | 5180 |
| F02 | `codex/ss-ui-f02` | `/private/tmp/salary-showdown-ui-worktrees/F02` | `2156f3a11c305600a2e58e0b4e483615d10081f2` | None |

Open each existing directory as the implementation session's workspace. Do not create another worktree or switch the original checkout. Both worktrees were clean at handoff. F01 app dependencies and F02 backend dependencies were installed with `npm ci --offline --no-audit --no-fund` using their unchanged lockfiles. I01 has both installations. No production environment files were copied; development uses the repository's local emulator defaults.

### Copyable F01 session prompt

```text
Implement Salary Showdown UI task F01 inline, without subagents, in the existing worktree /private/tmp/salary-showdown-ui-worktrees/F01 on codex/ss-ui-f01. The exact accepted bootstrap/base SHA is 2156f3a11c305600a2e58e0b4e483615d10081f2. Before editing, verify HEAD equals that SHA, the branch name is correct, and git status is clean; do not reset unexpected changes.

Read docs/superpowers/plans/salary-showdown-ui-tasks/F01.md, the linked design spec, master plan frozen contracts, and existing Salary Showdown HANDOFF in this worktree. Read the live coordinator ledger at /private/tmp/salary-showdown-ui-worktrees/I01/docs/superpowers/plans/salary-showdown-ui-tasks/completions/I01.md. Follow F01's exclusive ownership and preserve gameplay rules. Dependencies are already installed. Run app commands from games/salary-showdown/app; use npx vite --host 127.0.0.1 --port 5180 --strictPort. Unit checks may run concurrently. F02 has the first worker emulator slot, so any F01 emulator-backed browser mutation or suite must wait for a coordinator grant. Never restart emulators or clear shared data.

Complete the packet's checks, commit only owned files plus completions/F01.md, and report branch/base/head, test outcomes, screenshot paths, pending checks, and interface changes. The completion note may identify the implementation commit; report the final note-inclusive HEAD in your response. Do not edit another task, merge, deploy, or create sessions.
```

### Copyable F02 session prompt

```text
Implement Salary Showdown UI task F02 inline, without subagents, in the existing worktree /private/tmp/salary-showdown-ui-worktrees/F02 on codex/ss-ui-f02. The exact accepted bootstrap/base SHA is 2156f3a11c305600a2e58e0b4e483615d10081f2. Before editing, verify HEAD equals that SHA, the branch name is correct, and git status is clean; do not reset unexpected changes.

Read docs/superpowers/plans/salary-showdown-ui-tasks/F02.md, the linked design spec, master plan frozen contracts, and existing Salary Showdown HANDOFF in this worktree. Read the live coordinator ledger at /private/tmp/salary-showdown-ui-worktrees/I01/docs/superpowers/plans/salary-showdown-ui-tasks/completions/I01.md. Follow F02's exclusive ownership and preserve gameplay rules. Backend dependencies are installed. Run commands from games/salary-showdown/backend/functions; no Vite port is assigned.

F02 holds the first worker emulator slot after bootstrap. Check the live ledger before use. Tests that wrap this worktree's functions directly may use the shared DEV Firestore emulator at 127.0.0.1:8180. The Functions HTTP emulator currently serves the I01 checkout, so new setTeamIdentity HTTP/client verification remains pending until the coordinator pins your committed backend and confirms readiness. Do not restart emulators, edit their configuration, clear data, or run against production. Use fresh test game IDs and release the slot with your result.

Complete the packet's tests and commit only owned files plus completions/F02.md. Report branch/base/head, results, pending HTTP/client checks, and interface changes. The completion note may identify the implementation commit; report the final note-inclusive HEAD in your response. Do not edit frontend tasks, merge, deploy, or create sessions.
```

## Shared emulator service and reservation

- User explicitly confirmed the old processes were idle and authorized takeover. Old Firebase PID 10552 was gracefully stopped; original Firestore child was PID 10617.
- Current service owner: I01 coordinator. Backend checkout: `/private/tmp/salary-showdown-ui-worktrees/I01/games/salary-showdown/backend`, backend source at bootstrap SHA.
- Project: `salary-showdown-dev` only. Auth `127.0.0.1:9199`, Firestore `127.0.0.1:8180`, Functions `127.0.0.1:5101`, UI 4100, hub 4400, logging 4500, websocket 9150.
- Started using `firebase emulators:start --project salary-showdown-dev --only auth,functions,firestore`. At bootstrap, Firebase PID 15027, Firestore PID 15087, coordinator command session 32334. Recheck process ownership and readiness on resumption; PIDs are observations, not permanent identifiers.
- Log: `/private/tmp/salary-showdown-ui-worktrees/evidence/bootstrap/emulator.log`.
- Historical bootstrap reservation (superseded by the current gate above): F02 received the first exclusive worker slot and released it in its completion note. F01/F03 and all screen sessions require a later explicit grant for emulator suites or browser mutations. No coordinator mutable suites run while F02 holds this slot.
- Only one mutable verification slot at a time. No worker may reset emulator data or restart the service. Direct wrapped-function tests use their own worktree's JS; HTTP clients use the service's pinned backend checkout. These are distinct evidence sources.
- Before granting HTTP verification of F02, inspect its committed diff and completion note, then have I01 pin/restart the service against a stable committed candidate, with no other mutable slot active. Never hot-swap backend sources during a suite. No feature implementation belongs in the coordinator session.
- Request/release format: task ID, immutable commit, desired commands or browser scenarios, whether HTTP callable changes are needed, unique game IDs, then actual results and explicit release. Return those details to this coordinator task; no other session is created or messaged automatically.

## Baseline evidence

All baseline checks run in I01 against unchanged product code at bootstrap SHA.

| Check | Result |
| --- | --- |
| Frontend `npm test` | PASS: 15 files / 81 tests |
| Frontend `npx tsc -b --pretty false` | PASS, exit 0 |
| Frontend `npm run audit:ui` | PASS, 66 files scanned |
| Backend `npm test` | PASS: 27 files / 191 tests, local DEV emulators |
| Frontend `npx vitest run --config vitest.integration.config.ts` | PASS: 20 files / 38 tests, exit 0, 98.85 seconds |
| Build / new UI browser acceptance | Not run at bootstrap; required for final candidate |

Logs live at `/private/tmp/salary-showdown-ui-worktrees/evidence/bootstrap/frontend-unit.log`, `backend-unit.log`, and `frontend-integration.log`. Runtime: Node v24.16.0, npm 11.13.0. Backend declares Node 20; Firebase explicitly used host Node 24. This is a runtime-parity limitation, not evidence of Node 20 verification. No manifests or lockfiles were changed to silence warnings.

At bootstrap, no screenshot or new UI acceptance evidence existed and no implementation branch had been reviewed or merged. Current foundation results are recorded above. No deployment occurred.

## Dependency gates and merge policy

| Gate | Current status | Release condition / coordinator action |
| --- | --- | --- |
| Bootstrap → F01 + F02 | Worktrees ready | Publish exact bootstrap SHA and handoffs |
| F01 → F03 | Complete: corrected F03 merged at `ecdf667` | Review F01 ownership/contracts/results; merge its full accepted branch, run relevant checks, create F03 from the resulting immutable integration SHA, port 5182 |
| F01 + F02 + F03 → frozen base | Released: `b48cdf620c594aba0386fa6775af86aea40da401`; visual review deferred by user | All foundations reviewed/merged; frontend tests/typecheck/audit and backend identity tests pass; resolve outstanding foundation verification and API issues; create `codex/salary-showdown-ui-base` at exact accepted SHA |
| Frozen base → W01–W10 | Released: all ten worktrees prepared from the exact frozen base | Create all ten worktrees from that same SHA; record it and assigned ports before user starts sessions |
| Screen branches → I01 final acceptance | All ten merged; automated gates PASS; visual/classroom gate open | Review completion notes and full owned diffs; merge ready branches, preferably W01 through W10; no fragment cherry-picks |
| Integrated candidate | Closed | I01 acceptance matrix and all required checks pass; record single tested commit and remaining limitations; no deploy |

Screen ports W01–W10 are 5183–5192 respectively. Recommend 3–4 implementation sessions at a time; ownership permits ten. First visual review prioritizes W02 and W05.

Every review checks the submitted immutable head, correct base ancestry, explicit owned paths (using ownership.json plus permitted tightly local additions), unchanged dependency locks, test evidence, gameplay invariants, and completion note. Missing emulator/browser evidence is pending, not success. An implemented branch may merge into the isolated candidate with pending verification explicitly retained; it cannot pass final acceptance. Use whole-branch merges with clear provenance. Assign feature defects to their owner; only I01 integration failures and I01-owned files are coordinator implementation scope.

W08 preserves required shared chart props and default read-only behavior; W10 owns wrappers. W07 preserves StandingsTable required props. Contract exceptions must be recorded explicitly, reconciled at a gate, and communicated for affected user-managed branch rebases before work continues. Freeze shared foundation changes once screens start.

## Final I01 work, still pending

- Extend I01-owned rendered cross-screen regression coverage and preserve the full season suite.
- Full frontend suite, TypeScript, UI audit, backend suite, browser-SDK emulator integration suite, and build from the integrated commit.
- Complete two-team/five-round season; 21-team classroom drill with representative three-role clients.
- Every screen at required laptop viewports, 1024px fallback, 200% zoom, plus projector resolutions; before/after screenshot evidence.
- Keyboard placement/forms/focus; reduced motion at load and toggled; background return/reconnect; pending/rejected/revised/transition states; no early reveal or private-data leaks.
- Update HANDOFF/RUNBOOK with exact tested commit/project, real command results, limitations and rollback instructions. Local emulator evidence remains distinct from deployment evidence.
- Prepare backend-first then hosting release instructions for separately authorized deployment. Do not deploy, clean up worktrees, or implement another session's task.

This I01 gate adds only coordination-ledger changes beyond the whole F01/F02 merges. Coordinator interface changes: None. Worker interface additions are documented in their completion notes and match the frozen contracts. Final I01 status: pending F03, all screen tasks, and full final acceptance. No deployment or worktree cleanup occurred.


## Original F03 session prompt (historical; use correction prompt above)

```text
Implement Salary Showdown UI task F03 inline, without subagents, in the existing worktree /private/tmp/salary-showdown-ui-worktrees/F03 on codex/ss-ui-f03. The exact accepted starting SHA is 11c6c019221ea7d324571ba52e5d49be96caffe3; it contains the reviewed F01 and F02 branches. Before editing, verify HEAD equals that SHA, the branch is correct, and git status is clean. Do not reset unexpected changes, create another worktree, or use the original checkout.

Read docs/superpowers/plans/salary-showdown-ui-tasks/F03.md, the linked design spec, master plan frozen contracts, and existing Salary Showdown HANDOFF in this worktree. Read the LIVE coordinator ledger at /private/tmp/salary-showdown-ui-worktrees/I01/docs/superpowers/plans/salary-showdown-ui-tasks/completions/I01.md because this worktree's copied ledger predates your grant. Follow F03's exclusive ownership and preserve all gameplay and reveal/privacy rules. Dependencies are installed. Run app commands from games/salary-showdown/app; use npx vite --host 127.0.0.1 --port 5182 --strictPort.

I01 grants F03 the exclusive mutable emulator slot for the packet's integration tests and browser checks through completion or explicit release, subject to the live ledger. DEV project is salary-showdown-dev: Auth 127.0.0.1:9199, Firestore 127.0.0.1:8180, Functions 127.0.0.1:5101. The HTTP backend is pinned to I01's committed F01/F02 candidate and setTeamIdentity is verified. Recheck readiness; report an outage to I01 instead of restarting. Use fresh game IDs, run mutable checks sequentially, preserve the browser-SDK alias/config, and never clear shared data or use production. Record your actual tested commit/worktree state, commands, game IDs, results, and explicit slot release.

Complete the packet's unit, typecheck, UI-audit, emulator, and browser checks. Commit only owned files plus completions/F03.md. Report branch/base/final note-inclusive HEAD, changes, test outcomes, screenshot paths, pending checks, and any interface deviations. Do not edit another task, merge, deploy, or create sessions. Return your completion report to the I01 coordinator.
```
