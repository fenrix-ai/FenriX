# W04 completion — Auction showcase and sealed offer workflow

## Git

- Branch: `codex/ss-ui-w04`
- Frozen base: `b48cdf620c594aba0386fa6775af86aea40da401`
- Initial implementation commit: `3c1f4fcee06af48a1a1c22efb0fa3345b5a53465`
- Reviewed correction/source HEAD: `766409434bc0c9b02f6029232bb023e4ccd28f9c`
- Final note-inclusive HEAD: reported directly to I01 after this note commit (a commit cannot contain its own SHA).

## Changed files

- `games/salary-showdown/app/src/pages/AuctionPage.tsx`
- `games/salary-showdown/app/src/pages/AuctionPage.module.css`
- `games/salary-showdown/app/src/components/auction/OfferCard.tsx`
- `games/salary-showdown/app/src/components/auction/OfferCard.module.css`
- `games/salary-showdown/app/src/components/auction/OfferCard.test.tsx`
- `games/salary-showdown/app/src/components/auction/AuctionPage.test.tsx`
- `games/salary-showdown/app/src/lib/auctionDraft.ts`
- `games/salary-showdown/app/src/lib/auctionDraft.test.ts`
- `games/salary-showdown/app/src/itest/auction.itest.tsx`
- `docs/superpowers/plans/salary-showdown-ui-tasks/completions/W04.md`

## Delivered behavior

- Replaced the horizontal card strip with a responsive auction showcase and persistent offer-summary rail using the frozen navy/gold identity and shared `PlayerCard`.
- Shows raw player facts, per-round rate, duration, guaranteed commitment, total guaranteed commitments, and per-round worst-case payroll exposure.
- Keeps legal over-cap offer sheets enabled and labels the resolution skip behavior.
- Preserves exact raw and invalid edits across own-team live snapshots using per-player dirty state.
- Tracks per-player edit generations across pending submissions, so a response or saved snapshot cannot erase newer typing.
- Scopes editor state, Firestore listeners, pending responses, and action receipts to game/round/phase/team/role/auth UID.
- Sends a plain `{}` to withdraw all bids, never `null`.
- Shows SEALED only after callable success in the still-current scope; server rejection and newer pending edits never show a clean receipt.
- Keeps revisions available until the phase closes and leaves rival bids/results private.
- Supports visible focus, accessible number/radio controls, keyboard duration changes, reduced-motion CSS, and a single-column fallback below 1200px.

## Verification

All commands ran from `games/salary-showdown/app` unless noted.

- `npm test -- --no-file-parallelism` — PASS, 23 files / 120 tests.
- `npx vitest run --config /private/tmp/salary-showdown-ui-worktrees/evidence/i01-w04-review/vitest.review.config.mjs` — PASS, 1 file / 3 coordinator lifecycle regressions.
- `npx vitest run --config vitest.integration.config.ts src/itest/auction.itest.tsx` — PASS, 1 file / 1 integration test, final run 4.53s. The test uses the browser-SDK alias and fresh `salary-showdown-dev` games; it covers a two-client private snapshot overwrite while typing, min/step validation, legal over-cap submission, successful sealing, revision, `{}` withdrawal, and a phase-close race. The existing non-fatal Firebase Admin `MetadataLookupWarning: fetch failed` appeared.
- `npx tsc -b --pretty false` — PASS.
- `npm run audit:ui` — PASS, 85 files scanned.
- `npm run build` — PASS; existing bundle-size warning above 500 kB remains.
- `git diff --check` — PASS before both source commits.

## Browser/layout evidence

- Fresh DEV game `NVWLYW` was created through the local professor UI and advanced sequentially to round-1 auction.
- Default live browser inspection confirmed five showcase cards, 1.5-star and 5-star glyph examples, raw stats, large guarantees, keyboard ArrowRight duration selection, a legal `$190.0M` exposure warning, successful SEALED treatment, saved/unsaved transitions, and saved withdrawal of every offer.
- No screenshots were saved. The user deferred saved screenshots and the full integrated visual review to I01 acceptance.

## Pending final-acceptance checks

- Full integrated viewport matrix at 1280×720, 1366×768, 1440×900, and 1024px fallback.
- Actual 200% browser zoom.
- Real reduced-motion preference toggling (CSS behavior exists; no claim of an actual preference-toggle pass).
- Exact 3-star sample was not present in the inspected live wave; 1.5-star and 5-star samples were inspected.
- Final classroom/integrated privacy and cross-screen review by I01.

## Interfaces and limitations

- Interface deviations: None. `mergeAuctionSnapshot(draft, saved, dirty)` matches the frozen W04 contract.
- Runtime dependencies, manifests, lockfiles, shared contexts, shared hooks, backend code, and emulator configuration were unchanged.
- Emulator slot: released at completion; no emulator process was restarted, reconfigured, or cleared.
