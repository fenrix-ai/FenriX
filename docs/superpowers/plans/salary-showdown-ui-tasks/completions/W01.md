# W01 completion — Join, franchise identity editor and team lobby

Status: implemented; required automated behavior and bounded browser checks pass. Full visual/classroom acceptance remains deferred to I01. 2026-09-17.

## Provenance and scope

- Prepared worktree: `/private/tmp/salary-showdown-ui-worktrees/W01`.
- Branch: `codex/ss-ui-w01`.
- Verified clean starting HEAD / frozen base: `b48cdf620c594aba0386fa6775af86aea40da401`.
- Implementation commit: `df38e701b72dec8713bcf0910a7beccc39ce3672` (`feat(salary-showdown): rebuild join and lobby`).
- The documentation-only commit after the implementation includes this note. Its final note-inclusive HEAD is reported in the coordinator handoff because a commit cannot contain its own hash.
- Work was completed inline with no subagents, new worktrees, merges, service restarts, emulator clears, deployments, backend edits, manifest/lock changes, shared-context changes or frozen-interface changes.
- Interface deviations: **None**. `IdentityEditor({ identity, onSave, disabled })` matches the frozen signature and remains local to W01.

## Changed files

Under `games/salary-showdown/app/src/`:

- `pages/LandingPage.tsx`
- `pages/LandingPage.module.css`
- `pages/LobbyPage.tsx`
- `pages/LobbyPage.module.css`
- `components/lobby/IdentityEditor.tsx`
- `components/lobby/IdentityEditor.module.css` (task-local addition)
- `components/lobby/IdentityEditor.test.tsx`
- `components/lobby/RoleSeats.tsx`
- `components/lobby/RoleSeats.module.css` (task-local addition)
- `itest/landing.itest.tsx`
- `itest/lobby.itest.tsx`

Plus this completion note only.

## Implemented behavior

- Replaced the single-screen join picker with accessible League → Franchise → Role steps, a visible 1-of-3 progress indicator, focus transfer, back navigation, retained code/name/franchise inputs, server-backed lobby polling and late-join copy.
- Existing and newly created franchises share the same explicit role step. Claimed seats are labelled and disabled before an attempt. A stale open seat still goes through `joinGame`; the server conflict is rendered, the lobby is refreshed and the typed inputs remain intact.
- Preserved the hard membership invariant: both `joinGame` and `createTeam` resolve before `setGameId` runs. No create payload or callable contract changed.
- Added deterministic fallback franchise marks to old games without identity metadata. Lobby identity uses six named accent radios, three named jersey radios, local preview, explicit Save, pending/error/saved states and no click-to-autosave behavior.
- A rejected identity save retains its local preview. A successful save also retains the submitted preview while the team snapshot catches up. Clean editors follow teammate identity snapshots. Lobby/phase closure disables controls, and a racing rejected save retains its preview and error.
- Rebuilt the lobby around the player's own franchise: hero mark/name/code, three role seats with distinct Your seat / Claimed / Open seat labels, fallback explanation, teammate-arrival emphasis only after the initial seat snapshot, and the existing lobby-only rename flow.
- Removed LobbyPage's whole-room player listener. Own seats now consume frozen `useGame().teamSeats`; rival cards use public team documents only and do not imply private activity.
- Other franchises render in a numeric-aware compact grid. The factual house-rule ticker stops advancing under reduced motion, and CSS motion is restricted to `prefers-reduced-motion: no-preference`.

## Test-first evidence

- The required `IdentityEditor` test first failed because the component did not exist, then all four behavior tests failed against the compile-only shell because the radio and save controls were absent.
- The revised landing/lobby integration suite first failed 5/5 against the old one-screen picker and whole-room lobby.
- A later regression for saved-preview retention failed because the draft reverted to the stale prop after a successful callable. The effect now resyncs only on a new identity snapshot, and the regression passes.
- No existing assertion was deleted to make the suite green. The original create/join, rename, live server document and numeric ordering checks remain.

## Final verification

All app commands ran from `games/salary-showdown/app` in the W01 worktree.

| Check | Actual outcome |
| --- | --- |
| `npm test -- src/components/lobby/IdentityEditor.test.tsx` | PASS, 1 file / 4 tests |
| `npx vitest run --config vitest.integration.config.ts src/itest/landing.itest.tsx src/itest/lobby.itest.tsx` | PASS, 2 files / 5 tests, 10.92s |
| `npm test` | PASS, 21 files / 114 tests |
| `npx tsc -b --pretty false` | PASS, exit 0 |
| `npm run audit:ui` | PASS, 87 files scanned |
| `git diff --check` | PASS |

The final integration run emitted the existing non-fatal Firebase Admin `MetadataLookupWarning` twice. No assertion retries, timeout changes, browser-SDK alias changes or emulator configuration changes were made.

Integration cases cover the three steps, explicit taken-seat explanation, stale-seat server rejection and refresh, create-before-route ordering, old-game identity fallback, own/open role labels, unclaimed-role fallback, post-snapshot teammate arrival, teammate identity snapshot propagation, rename authority and numeric rival ordering. Each test creates a fresh DEV game through the existing harness; the harness does not emit those game IDs.

## Browser behavior and deferred visual work

- Started the assigned preview exactly with `npx vite --host 127.0.0.1 --port 5183 --strictPort`.
- Fresh browser fixture: game `zkuVvQ4enCi1ymR00xFZ`, join code `ZKUVVQ`, project `salary-showdown-dev` only.
- Keyboard progression created Northstars and claimed GM. Two more isolated browser identities claimed Scout and Coach. The rendered lobby showed all three named occupants with distinct own/claimed labels.
- A fourth rendered browser identity joined Comets. The professor view confirmed both Northstars and Comets had all three roles claimed. The browser identity saved Teal + Stripe and the Northstars Scout client received both values from the live team snapshot.
- Two additional same-origin in-app-browser tabs completed Comets mutations on the server, but their client listeners did not hydrate the lobby. Their browser console error lists were empty; a separate `localhost` origin rendered Comets successfully. This browser-provider/concurrency observation is not promoted to a pass for high-tab-count classroom behavior.
- Screenshot paths: **None**. Per the user/I01 deferral, 21-team layout, saved screenshots, exact laptop viewport visual inspection, 1024 fallback, actual 200% zoom, actual reduced-motion preference toggling and full classroom review remain **pending** for integrated acceptance. Automated tests and reduced-motion CSS are supporting behavior evidence only.

## Emulator slot release

W01 checked the coordinator-owned service through approved localhost access: hub `4400`, Auth `9199` and Firestore `8180` returned HTTP 200; Functions `5101` returned the expected root HTTP 404 response. All W01 mutations used fresh DEV games and ran sequentially. W01 did not restart, reconfigure or clear any emulator and did not touch production.

**W01 explicitly releases the exclusive mutable emulator slot with this completion report.** W01 will perform no further emulator mutation without a new coordinator grant.
