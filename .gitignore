.archive/
node_modules/
.env
.env.local
.env.production
dist/
build/
.firebase/
__pycache__/
*.pyc
.DS_Store
.vscode/
!.vscode/settings.json
.cursor/
*.log
.vercel
.claude/worktrees/
.worktrees/
.superpowers/

# Bakery Bash — local snapshot tool output (test artifacts)
games/bakery-bash/backend/snapshots/

# Bakery Bash — professor / student dataset files. Watermarked
# "for educational use only" by the publisher and not redistributable.
# Scripts in analysis/scripts/ expect copies to live here locally; do
# not commit them to this public repo.
analysis/data/

# Tim's personal notes and transcripts (local-only)
_notes/
agent_context/
docs/chapman/
meetings_transcripts/

# Quant finance work — keep the dir skeletons, ignore bulk/generated content
# Challenge data: keep the folder, ignore everything inside it
quant_finance/data/*
!quant_finance/data/.gitkeep
# Model weights are huge — never commit them.
# (registry.yaml, scripts/, cards/, README.md, AND results/ under models/ ARE
#  tracked: small config + benchmark outputs + the report go to GitHub.)
quant_finance/models/gguf/
